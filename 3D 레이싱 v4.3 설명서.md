# 3D 레이싱 v4.3 — 설명서

`3D 레이싱 v4.3.ent`는 **폴리곤 그리는 순서 버그**를 고친 작은 업데이트입니다. 서킷 설명은 `old/3D 레이싱 v4.0 설명서.md`, 다리·지형은 `old/3D 레이싱 v4.2 설명서.md`, 조작은 `3D 레이싱 사용법과 게임 소개.md`를 보세요.

## 무엇이 문제였나
이 게임은 먼 것부터 가까운 것 순서로 칠하는 방식(화가 알고리즘)으로 3D를 그립니다. 트랙은 링 1–3개씩 묶은 단위로 멀리서부터 그리고, 건물과 나무는 옆 링의 단위에 함께 그립니다. 여기서 두 가지 순서가 틀려 있었습니다.
- **같은 단위 안에서**: 도로와 벽을 먼저, 건물을 나중에 그려서 **벽 뒤의 건물이 벽을 덮었습니다.** 모나코 터널 위 건물도 터널 천장을 덮었습니다.
- **긴 건물**: 도로를 따라 긴 건물은 가장 가까운 링 하나에만 매달려 있어서, 건물의 먼 쪽 끝이 그보다 먼 링의 벽(먼저 그려진)을 덮었습니다.

## 이렇게 고쳤습니다
- 단위마다 **땅(잔디·런오프) → 도로 뒤의 풍경 → 도로·연석·벽·터널 → 차 → 카메라와 도로 사이의 풍경** 순서로 그립니다. 마지막 단계는 다른 구간(헤어핀 건너편 등)을 옆에서 볼 때만 쓰입니다.
- 풍경마다 도로의 어느 쪽에 얼마나 떨어져 서 있는지 기억하고, 같은 링에서는 도로에서 먼 것부터 그립니다.
- 긴 건물은 양 끝 링에 모두 매달고, 매 프레임 **카메라에서 더 먼 쪽 끝 링**에서 한 번만 그립니다.

모나코와 바쿠에서 같은 위치를 전후로 비교했을 때, 벽을 덮던 건물들이 이제 벽 뒤로 들어갑니다.

## 확인한 것
- v3.0 기능 검사 10개, 키, 차체 기울기, 벽에 끼인 차, 리스트 채우기, 풍경 침범 검사 통과.
- 프레임률: 모나코 ULTRA에서 v4.2와 같은 수준(tessvm, 측정 오차 안).
