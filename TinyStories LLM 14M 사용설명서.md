# TinyStories LLM 14M in Entry — 사용설명서

TinyStories(어린이 동화) 말뭉치로 학습된 오픈웨이트 언어모델 **delphi-suite/v0-llama2-12.8m**
(1,456만 파라미터, Llama-2 구조, MIT)을 **엔트리 블록만으로** 돌리는 작품입니다. 외부 확장이나
서버 없이 작품 안의 리스트·변수·계산 블록만 사용합니다.

**playentry.org에 올릴 수 있습니다.** 작품 문서 크기가 14.2MB로, 사이트 한도(약 16MB) 아래입니다.
(같은 방식으로 만든 SmolLM2-135M 판은 92.7MB여서 사이트에는 올라가지 않습니다 — 로컬 전용입니다.)

## 사용법

1. 작품을 열고 ▶ 시작을 누릅니다. (불러오는 데 10초 정도 걸립니다.)
2. "준비 완료"가 뜨면 입력창에 **영어 문장의 시작**을 입력합니다. 예: `Once upon a time`
3. 프롬프트를 한 토큰씩 읽고(상태줄: `프롬프트 읽는 중`), 이어서 글을 씁니다(`생성 중`).
   새 조각이 나올 때마다 화면에 이어 붙습니다.
4. 다 쓰면 다시 입력창이 나옵니다.

**속도**: 토큰 하나에 **약 70초**(레이어 8개, 레이어당 약 8.7초) — 다른 창을 여러 개 띄운 PC에서
측정한 값이고, 브라우저가 한가하면 30~40초까지 줄어듭니다. 프롬프트 4토큰 + 생성 20토큰이면
15~30분입니다. 참고로 같은 방식의 SmolLM2-135M 판은 같은 PC에서 토큰당 약 300초였습니다.
상태줄에 지금 몇 번째 토큰의 몇 번째 레이어를 계산 중인지와 경과 시간이 나옵니다.

## 나오는 글

greedy(temp=0)로 `Once upon a time`을 주면 이렇게 씁니다:

> Once upon a time, there was a little girl named Lily. She had a big, red ball.
> Lily loved to play with her ball every day.

파이토치 원본과 **토큰 단위로 같은 결과**임을 시뮬레이터와 참조 구현으로 확인했습니다.

## 바꿀 수 있는 변수

| 변수 | 기본값 | 뜻 |
|---|---|---|
| `maxNew` | 32 | 한 번에 생성할 토큰 수 |
| `temp` | 0.7 | 온도. 0이면 항상 가장 확률 높은 토큰, 높을수록 다양 |
| `repPen` | 1.3 | 반복 페널티. 이미 나온 토큰의 점수를 이만큼 깎음 |
| `chunkRows` | 1024 | 몇 행마다 화면을 갱신할지 |

## 알아둘 점

* **영어 동화 전용 모델**입니다. 어휘가 4,096개뿐이고 TinyStories(3~4세 수준 영어)만 배웠습니다.
  한글을 넣으면 바이트 단위로 쪼개 처리하긴 하지만 의미 있는 답은 나오지 않습니다.
* 가중치는 **2.67비트**로 압축돼 있습니다(가중치 3개 = 문자 1개). 8,000만 토큰 양자화 인지 학습(QAT)
  으로 보정해서, 검증 perplexity가 원본 4.375 → **4.78**입니다.
* 변수/리스트 값이 크기 때문에 엔트리 편집기의 **속성 > 변수/리스트 화면에서 값을 직접 붙여넣거나
  수정하지 마세요**(페이지가 죽습니다). 값은 파일에 이미 들어 있습니다.
* 출처와 라이선스는 `TinyStories-LLM-LICENSES.txt`에 있습니다.
