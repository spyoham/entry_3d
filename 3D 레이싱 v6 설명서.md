# 3D 레이싱 v6 — 설명서

`3D 레이싱 v6.ent` 는 v5(F1 에디션)의 그래픽 수준(렌더 거리·폴리곤·풍경 밀도·LOW/HIGH/ULTRA)을 그대로 두고
**최적화, 구조물 배치, 오브젝트 수, 메뉴**를 고친 버전입니다. v1–v5 파일은 그대로 남아 있습니다.
조작과 게임 규칙은 v5와 같습니다(`3D 레이싱 v5 설명서.md` 참고).

## 요청별 변경

### 1. 구조물이 트랙 안으로 삐져나오던 문제
- 원인: 건물·나무를 "도로 중심에서 d m" 로만 놓아서, **모델 크기**(큰 빌딩은 폭 30 m 이상)와
  **다른 구간의 트랙**(헤어핀, 나란히 달리는 직선, 교차로)을 고려하지 않았습니다.
  8개 서킷에서 약 240개가 도로·연석·런오프에 걸쳐 있었습니다(싱가포르 빌딩이 다른 직선 위에 서 있는 등).
- 수정: 트랙을 만들 때 모든 구조물의 **실제 바닥 크기(모델 상자 × 배율, 회전 포함)**를 **서킷 전체**와 비교합니다.
  도로·연석·런오프에 닿으면 바깥으로 옮기고(최대 3번), 그래도 안 되면 그 자리에는 놓지 않습니다.
  이제 트랙과 겹치는 것은 일부러 도로 위에 있는 것(광고 갠트리, 스즈카 입체교차 다리, 모나코 터널 위 호텔)뿐입니다.
- 몬자의 "옛 뱅킹"은 도로를 가로막는 벽으로 놓여 있었는데(차가 통과함), 트랙 옆에 트랙을 향해 세웠습니다.

### 2. 렌더 거리·그래픽을 낮추지 않은 3D 최적화
tessvm 실행기를 직접 받아 분석하고 프로파일한 결과, tessvm은 엔트리의 **십진수 사칙연산**을 정확히 흉내 내느라
소수끼리의 뺄셈·덧셈이 매우 비쌌습니다(프레임 시간의 약 70%가 정점 변환의 이 연산).
- **정수 고정소수점 3D 변환**: 월드 좌표를 cm 정수, 카메라 축을 16384배 정수로 바꿔 정점마다의 내적이 순수 정수 연산이 되게 했습니다.
  화면 좌표도 1/16 픽셀 정수로 다룹니다(뒷면 판정·화면 밖 판정이 정수 연산). 화질 차이는 없습니다.
- **시야 절두체 컬링**: 화면 밖에 있는 풍경 오브젝트·차는 정점 변환 전에 경계구로 걸러냅니다.
- **느린 PC에서의 슬로모션 제거**: tessvm은 시계가 "틱 수 × 1/60초"라서 60 fps를 못 내면 게임 전체가 느려졌습니다.
  tessvm에서는(`$TESSVM` 변수) 실제 초당 틱 수를 재서 시간 배율을 맞춥니다.

측정(이 PC, tessvm, 헤드리스 크롬):

| 상황 | v5 | v6 |
|---|---|---|
| 싱가포르 ULTRA 레이스 (틱당) | 13–21 ms (37–50 fps) | **5.1–5.9 ms (60 fps)** |
| 모나코 HIGH 레이스 (틱당) | 9–15 ms | **4.3–4.6 ms** |
| CPU 4배 느리게 (ULTRA) | 14 fps, 게임 속도 약 0.24배 | **34 fps, 게임 속도 실시간** |
| 순정 엔트리 HIGH 레이스 | 1.9 fps | **2.55 fps** |

### 3. 오브젝트 최소화 (복제본 사용)
- v5: 글상자 32개 + 펜 1개 = **33개** → v6: **글상자 1개 + 펜 1개 = 2개**.
- 글상자 오브젝트가 시작할 때 자기 복제본 64개를 만들고, 복제본마다 "이 오브젝트에서 사용" 변수(`txt$slot`)로
  자기 칸 번호를 기억합니다. 화면 글자는 `txS/txX/txY/txZ/txC/txV` 리스트의 칸에 쓰고,
  복제본은 자기 칸의 버전(`txV`)이 바뀌었을 때만 다시 그립니다.
- 스크립트 묶음도 33개 → 3개로 줄었습니다.

### 4. 메뉴 재디자인
- **메인 메뉴**: 왼쪽에 기울어진 메뉴 바(선택하면 빨간 바가 미끄러져 나옴), 오른쪽에 선택 항목의 **미리보기 카드**.
  - RACE START: 회전하는 **3D 서킷 지도** + 레이스 설정 요약
  - CAR: 카드 안에서 도는 **3D 차 모델** + 성능 막대 4개
  - CIRCUIT: 3D 지도 + 길이/코너 수/DRS 존/최고 랩
  - MODE / AI / LAPS / WEATHER(해·비 아이콘) / GRAPHICS / EDITOR 각각 설명 카드
- **차 선택(차고)**: 출발선 위 **턴테이블**에서 차가 돌고, 오른쪽 패널에 **성능 그래프**
  (최고속·가속·코너링·다운포스 막대, 다른 차의 값은 눈금으로 비교)와 **제원표**
  (최고속, 0-100·0-200 km/h, 100/250 km/h 횡G, 무게 — 게임 물리식으로 계산한 값).
- **서킷 선택**: 고저차를 과장한 **회전 3D 지도**(높은 곳일수록 밝게) + 길이, 코너, DRS 존, 고저차, 형태, 기록.
- 일시정지·결과·순위표도 같은 스타일의 패널로 바꿨습니다.

### 5. 메뉴 배경 회전이 끊기던 문제
- 원인: 배경 카메라가 "18구간 앞의 링"을 정수 단위로 조준해서, 링을 지날 때마다 시선이 툭 튀었습니다.
- 수정: 눈과 조준점을 모두 구간 안에서 연속으로 보간하고, 방향을 부드럽게 따라가게 했습니다.
  60 fps 기준 프레임 간 방향 급변(저크)이 최대 약 4° → 0.01° 미만.

## 참고
- 글자는 "나눔고딕코딩" 글꼴을 씁니다. 값은 왼쪽 정렬 열로 배치해서 글꼴이 달라도 표가 흐트러지지 않습니다.
- 순정 엔트리에서는 여전히 GRAPHICS LOW를 권장합니다. tessvm에서는 ULTRA도 60 fps가 나옵니다.
