# SmolLM2-135M in Entry — 사용설명서

HuggingFace의 오픈소스·오픈웨이트 언어모델 **SmolLM2-135M**(1억 3500만 파라미터, Apache-2.0)을
**엔트리 블록만으로** 돌리는 작품입니다. 외부 확장이나 서버 없이, 작품 안의 리스트·변수·계산 블록만
사용합니다.

## 사용법

1. 작품을 열고 ▶ 시작을 누릅니다. (파일이 커서 불러오는 데 10~30초 걸립니다.)
2. "준비 완료"가 뜨면 입력창에 **영어 문장의 시작**을 입력합니다. 예: `Once upon a time`
3. 모델이 프롬프트를 한 토큰씩 읽고(상태줄: `프롬프트 읽는 중`), 이어서 글을 씁니다(`생성 중`).
   새 단어가 나올 때마다 화면에 이어 붙습니다.
4. 다 쓰면 다시 입력창이 나옵니다. 새 입력은 새 글로 시작합니다.

**속도**: 토큰(단어 조각) 하나에 **약 3~4분** 걸립니다. 프롬프트 4토큰 + 생성 20토큰이면 1시간 20분 정도입니다.
1억 3500만 개의 가중치를 토큰마다 모두 읽어야 하기 때문입니다. 상태줄에 지금 몇 번째 토큰의 몇 번째
레이어(1~30)를 계산 중인지와 경과 시간이 나옵니다.

## 바꿀 수 있는 변수

| 변수 | 기본값 | 뜻 |
|---|---|---|
| `maxNew` | 32 | 한 번에 생성할 토큰 수 |
| `temp` | 0.7 | 온도. 0이면 항상 가장 확률 높은 토큰(반복이 심해짐), 높을수록 다양 |
| `repPen` | 1.3 | 반복 페널티. 이미 나온 토큰의 점수를 이만큼 깎음 |
| `chunkRows` | 1024 | 몇 행 계산할 때마다 화면을 갱신할지 (작을수록 화면이 자주 바뀌지만 조금 느려짐) |

## 알아둘 점

* **영어 모델**입니다. 한글도 입력은 되지만(UTF-8 바이트로 변환) 의미 있는 답은 기대하기 어렵습니다.
* 채팅 모델이 아니라 **글 이어쓰기** 모델입니다. 질문보다 문장의 시작을 주는 편이 좋습니다.
* 평균 2.67비트(열마다 3·3·2비트)로 줄인 모델이라 원본보다 약간 덜 매끄럽습니다(WikiText-2 perplexity 원본 20 → 이 작품 약 33).
* 문맥은 최대 128토큰(프롬프트 + 생성)입니다.
* 작품 파일이 약 70MB입니다. 엔트리 오프라인 에디터에서 확인했습니다. 온라인 업로드 용량 제한은
  확인하지 못했습니다.

## 어떻게 5000칸 리스트에 1억 개를 넣었나

* **가중치 3개 = 글자 1개.** 가중치 3개를 3·3·2비트(8·8·4단계)로 묶으면 256가지 = Latin-1 글자
  하나가 됩니다. 행렬 한 행(576개)은 192글자짜리 문자열이고, 행렬 하나가 통째로 문자열 하나입니다.
  리스트 `WALL`에는 행렬 120개가 120칸에 들어 있습니다(각 칸이 수십만~수백만 글자).
* **덧셈 대신 "합치기".** 엔트리의 `+`는 정밀 계산 라이브러리를 거쳐 느립니다(블록당 약 2µs).
  그래서 곱셈 결과를 *길이가 그 값인 문자열*(예: 값 5 → `IIIII`)로 표에 미리 만들어 두고,
  한 행의 결과는 표에서 꺼낸 문자열들을 `(A)와 (B) 합치기`로 이어 붙인 뒤 `(글자)의 길이`로 잽니다.
  브라우저는 이어 붙이기를 복사 없이 처리하므로 값이 수억이어도 블록 하나에 0.2µs 정도입니다.
* **표 조회 한 번 = 가중치 3개.** 행의 글자를 `(글자)의 (n)번째 글자`로 꺼내고, `위치` 블록으로 번호를
  구해 표 리스트에서 문자열을 꺼냅니다. 표는 입력 벡터가 바뀔 때마다 다시 만듭니다(48열마다 4096칸).
* **정밀도**: 표 값은 최대 약 700만 단위의 정수라서, 엔트리 안의 계산 결과가 PyTorch로 계산한 같은
  양자화 모델과 소수점 넷째 자리 안팎까지 일치합니다(같은 문장을 생성함을 확인).
* 토크나이저(BPE 48900개 병합 규칙), UTF-8 디코더, 어텐션(KV 캐시는 고정폭 숫자 문자열),
  RoPE, SiLU(보간 표), top-k 샘플링도 모두 블록입니다.

## 출처와 라이선스

* 모델: HuggingFaceTB/SmolLM2-135M (Apache License 2.0) — 이 작품은 그 가중치를 평균 2.67비트로 양자화하고
  원본 모델을 교사로 추가 학습(지식 증류)한 **수정본**입니다.
* 양자화 학습 데이터: HuggingFaceTB/smollm-corpus (FineWeb-Edu-dedup, Cosmopedia-v2) 일부.
* 자세한 고지는 같은 폴더의 `SmolLM2-LICENSE.txt`.
