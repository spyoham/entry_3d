# 3D 엔진 v4 — 사용설명서

`3D v4.ent` 는 **엔트리 블록만으로 3D 게임을 만들 수 있게 해 주는 엔진**입니다.
프로젝트를 열면 함수 목록에 85개의 한글 3D 함수가 들어 있고, `게임` 오브젝트에는
그 함수들로 만든 예제 게임(별 모으기)이 들어 있습니다.

예제를 지우고 자기 게임을 그 자리에 만들면 됩니다. 함수는 프로젝트 전체에서
쓸 수 있으므로 **어떤 오브젝트에서 불러도 됩니다** (단, `3D 장면 그리기` 는 붓을 쓰는
오브젝트 = `게임` 오브젝트에서 부르세요).

---

## 1. 5분 만에 시작하기

가장 짧은 3D 프로그램입니다. `게임` 오브젝트의 코드를 다음처럼 바꿔 보세요.

```
시작하기 버튼을 클릭했을 때
  🟦3D 엔진 시작하기
  📦바닥 만들기  크기 60  색 [잔디]
  📦상자 만들기  x 0  y 1  z 8   가로 2  높이 2  세로 2   색 [빨강]
  계속 반복하기
    🟦프레임 시작하기
    🎥카메라를  좌우 (0.5)  상하 (0) 만큼 돌리기
    🟦3D 장면 그리기
```

이것만으로 빨간 상자 주위를 도는 3D 화면이 나옵니다.

**게임 한 판의 기본 뼈대**는 항상 이 순서입니다.

```
시작하기 버튼을 클릭했을 때
  🟦3D 엔진 시작하기          ← 딱 한 번
  (월드 만들기: 바닥, 건물, 캐릭터 …)
  계속 반복하기
    🟦프레임 시작하기          ← 시간·키보드 읽기
    (내 게임 규칙: 키 처리, 점수, 적 움직이기 …)
    ⚙️물리 계산하기            ← 물리를 쓰면
    🎥카메라 3인칭 …           ← 카메라 결정
    🟦3D 장면 그리기            ← 화면 그리기
```

---

## 2. 꼭 알아야 할 5가지

### (1) 좌표계
* **x** = 오른쪽/왼쪽, **y** = 위/아래(높이), **z** = 앞/뒤.
* 각도는 도(°) 단위. `카메라 좌우각` 0° 는 +z 방향(앞)을 봅니다.
* 오브젝트의 위치 (x, y, z) 는 **물체의 한가운데**입니다.
  높이 2인 상자를 바닥에 딱 붙이려면 y 를 1로 두세요.
  (`🔶오브젝트 ( ) 을 바닥 높이 ( ) 에 세우기` 를 쓰면 자동으로 맞춰 줍니다.)

### (2) 오브젝트 번호
무엇을 만들든 **번호(1, 2, 3 …)** 가 하나 붙습니다. 방금 만든 번호는 변수
**`만든오브젝트`** 에 들어갑니다. 그래서 보통 이렇게 씁니다.

```
📦사람 만들기 x 0 y 2 z 0  키 1.8  색 [파랑]
[내캐릭터] 를 (만든오브젝트) 로 정하기
⚙️오브젝트 (내캐릭터) 의 물리를 1 로 정하기
```

### (3) 색
색 칸에는 **한글 색 이름**이나 **`#rrggbb`** 를 적습니다.
쓸 수 있는 이름은 리스트 `색이름` 에 다 들어 있습니다:

> 빨강 주황 노랑 연두 초록 청록 하늘 파랑 남색 보라 분홍 자주 갈색 살구 검정
> 회색 하양 흰색 금색 은색 잔디 흙 모래 바다 벽돌 돌 나무 밤 어두운회색
> 밝은회색 크림 민트

### (4) 오브젝트 상태는 리스트로 읽고 씁니다
엔트리의 "결과값 함수"는 호출 한 번에 약 2프레임이 걸려서(엔진이 느려집니다)
이 엔진은 **모든 상태를 한글 리스트/변수로 열어 두었습니다.** 엔트리 기본
블록으로 바로 읽고 쓸 수 있습니다.

| 리스트 | 뜻 |
|---|---|
| `오브젝트X` `오브젝트Y` `오브젝트Z` | 위치 |
| `오브젝트회전X/Y/Z` | 회전(도) |
| `오브젝트크기X/Y/Z` | 크기 |
| `오브젝트속도X/Y/Z` | 속도 |
| `오브젝트보임` | 1 보임 / 0 숨김 |
| `오브젝트땅닿음` | 1이면 바닥에 서 있음 |
| `오브젝트닿은것` | 이번 프레임에 닿은 상대 번호(0이면 없음) |
| `오브젝트이름표` | 내가 붙인 이름 |
| `오브젝트물리` `오브젝트모양` | 물리 종류 / 모양 번호 |
| `충돌A` `충돌B` | 이번 프레임에 부딪힌 짝 목록 (`충돌수` 개) |
| `화면글` | 화면 글 8칸의 내용 |

예) 플레이어의 x 좌표 = `오브젝트X 의 (내캐릭터) 번째 항목`

주요 변수: `프레임시간`(dt) `흐른시간` `FPS` `오브젝트수` `그린면수`
`카메라X/Y/Z` `카메라좌우각` `카메라상하각` `카메라시야각` `중력`
`만든오브젝트` `만든모양` `거리` `방향각` `닿음`
`광선맞음` `광선오브젝트` `광선거리` `광선X/Y/Z` `화면X` `화면Y` `화면앞`
`입력앞뒤` `입력좌우` `입력점프` `엔진알림`(용량이 꽉 찼을 때의 안내)

### (5) 움직임은 `프레임시간` 을 곱하세요
프레임 속도는 장면마다 다릅니다. `초당 3` 만큼 돌리고 싶으면
`3 × 프레임시간` 을 쓰세요. 그래야 느린 컴퓨터에서도 같은 속도로 움직입니다.

---

## 3. 함수 설명

### 🟦 엔진
| 함수 | 설명 |
|---|---|
| 3D 엔진 시작하기 | 한 번 호출. 시계·카메라·빛·붓을 초기화합니다 |
| 3D 월드 비우기 | 오브젝트를 전부 지웁니다(다시 시작할 때) |
| 프레임 시작하기 | 프레임시간 계산 + 키보드 상태 읽기 |
| 3D 장면 그리기 | 하늘 → 물체 → 그림자를 원근으로 그립니다 |

### 📦 오브젝트 만들기
상자 · 공 · 원기둥 · 원뿔 · 피라미드 · 경사면 · 바닥 · 네모판 · 원판 · 별 ·
나무 · 사람 · (내가 만든 모양) · 복제 · 지우기.

* **경사면**의 `방향` 은 0 / 90 / 180 / 270 (도). 캐릭터가 **걸어 올라갈 수 있는
  진짜 비탈**입니다.
* **바닥 만들기**는 타일 25장으로 된 바닥 + 보이지 않는 충돌 판을 만듭니다.
  (안개가 자연스럽게 걸리고, 물체가 바닥에 묻히지 않습니다.)
* **네모판·원판·별**은 종잇장처럼 얇은 판이라 양면 모두 보입니다.
  `🔶오브젝트 ( ) 항상 카메라 보기 1` 을 켜면 항상 정면을 보는 빌보드가 됩니다.

### 🔷 내 모양 디자인하기
```
🔷새 모양 만들기 시작
🔷모양에 점 추가 x -0.5 y -0.5 z -0.5      ← 1번 점
🔷모양에 점 추가 …                          ← 2번, 3번 …
🔷모양에 면 추가  점 1 2 3 4  색 [빨강]     ← 점 번호로 면을 만듭니다
🔷모양에 면 추가  점 1 2 3 0  색 [노랑]     ← 4번째를 0으로 두면 삼각형
🔷모양 완성하기                             ← 만든모양 에 번호가 들어옵니다
📦모양 (만든모양) 번으로 만들기 x 0 y 2 z 5 크기 4 색 [하양]
```
* 점 좌표는 **-0.5 ~ 0.5 정도**로 만드세요. 그 모양이 `크기` 만큼 커집니다.
* 면의 색을 비워 두면 오브젝트 색을 씁니다.
* 점 순서를 거꾸로 적어도 됩니다(내가 만든 면은 양면이라 항상 보입니다).
* `🔷모양 ( ) 의 먼 거리 모양을 ( ) 번으로 정하기` 로 멀리 있을 때 쓸
  간단한 모양을 지정하면 빨라집니다.

### 🔶 오브젝트 다루기
위치/이동/회전/크기/색/숨기기/이름표/그림자/빌보드/빛무시/그리기층/충돌크기,
바라보는 쪽으로 가기, 무엇을 바라보게 하기, 바닥에 세우기.

* **그리기 층**: -1은 항상 뒤(바닥용), 0 보통, 1은 항상 앞.
* **빛 무시**: 아이템처럼 항상 같은 밝기로 빛나게 합니다.

### ⚙️ 물리
`오브젝트 ( ) 의 물리를 ( ) 로 정하기` 의 숫자:

| 값 | 종류 | 쓰임 |
|---|---|---|
| 0 | 없음 | 장식 |
| 1 | 움직임 | 캐릭터, 공, 총알 (중력·충돌) |
| 2 | 고정 | 땅, 벽, 건물, 경사면 |
| 3 | 센서 | 통과하지만 닿은 것은 알려 줌 (코인, 골인 지점) |
| 4 | 움직이는 발판 | 내가 직접 위치를 바꾸는 엘리베이터 (그 위의 캐릭터가 같이 실려 갑니다) |

* 튕김(0~1), 마찰, 무게, 속도, 힘, 점프, 공 충돌 모양을 정할 수 있습니다.
* `⚙️물리 계산하기` 한 번이 한 프레임입니다(내부에서 잘게 나눠 계산).
* `⚙️캐릭터 ( ) 움직이기` / `⚙️오브젝트 ( ) 을 키보드로 조종하기` 는
  **카메라가 보는 방향 기준**으로 움직입니다(3인칭 게임의 기본).
* `⚙️광선 쏘기` → `광선맞음/광선오브젝트/광선거리/광선X,Y,Z` 로 결과가 옵니다.
  총 쏘기, 시선 앞의 물체 알아내기, 바닥 높이 재기에 씁니다.
* `⚙️세상 경계` 를 정하면 보이지 않는 벽이 생깁니다.

### 🎥 카메라
위치·각도·돌리기·바라보기·시야각·흔들기, **1인칭**, **3인칭**,
마우스(누른 채 끌기)·방향키 시점.

* `🎥카메라 3인칭` 의 `부드럽게` 는 0(딱 붙음)~0.9(천천히 따라옴).
  벽이 끼면 카메라가 알아서 앞으로 당겨집니다.
* `🎥카메라 1인칭` 은 캐릭터 눈높이에 카메라를 두고, 캐릭터가 카메라 쪽을
  바라보게 합니다(보통 캐릭터는 숨깁니다).

### 🌈 화면·빛
하늘색(위/아래/땅), 빛 방향과 밝기, 보이는 거리(=안개), 최대 면 수,
그리기 방식(0 면 / 1 선 / 2 면+선), 그림자, 월드 점의 화면 위치 구하기.

### 🖥️ HUD(화면 앞 표시)
네모·선·막대(체력바)·조준점 그리기와 **글 8칸**.
글은 `🖥️글 ( ) 번에 ( ) 쓰기` 로 쓰고, `🖥️글 ( ) 번을 x y 색 로 놓기` 로
자리를 정합니다. `🖥️글 ( ) 번을 월드 ( )( )( ) 위치에 ( ) 로 띄우기` 는
3D 공간의 한 점 위에 글자를 띄웁니다(이름표, 데미지 숫자).

### 📐 계산 도우미
두 오브젝트/두 점 사이 거리 → `거리`, 방향각 → `방향각`,
오브젝트를 다른 오브젝트 쪽으로 가게 하기(적 추적 한 블록).

---

## 4. 예제 게임 조작

| 키 | 기능 |
|---|---|
| W A S D | 이동 (카메라 기준) |
| SPACE | 점프 |
| 마우스 끌기 / ← → | 시점 돌리기 |
| C | 1인칭 ↔ 3인칭 |
| F | 공 쏘기 |
| R | 다시 시작 |

별 8개를 모으면 클리어. 1인칭에서는 조준점이 가리키는 물체의 이름표가 뜹니다.
예제 안에는 경사면 오르기, 움직이는 발판, 튕기는 공, 직접 만든 모양(오두막),
센서(별), 광선, 그림자, 안개가 모두 들어 있습니다.

---

## 5. 빠르게 만드는 요령

엔트리는 블록 하나하나가 느리기 때문에 **화면에 그리는 면의 수**가 속도를
결정합니다. 느리면 이 순서로 줄이세요.

1. `🌈보이는 거리를 ( ) 로 정하기` 를 줄인다 (예: 120 → 70). 제일 효과가 큽니다.
2. `🌈그림자 0` 으로 끄거나 `🌈그림자가 보이는 거리` 를 줄인다.
3. 멀리 있는 물체에 `🔷먼 거리 모양` 을 지정한다 (공·나무·사람은 이미 되어 있음).
4. 물체 수를 줄이거나, 안 보이는 것은 `보이기/숨기기 0` 으로 끈다.
5. `🌈한 화면 최대 면 수` 를 줄이면 강제로 잘립니다(멀리 있는 면부터 사라짐).
6. 공·나무처럼 면이 많은 모양 대신 상자·피라미드를 쓴다.

참고 수치(헤드리스 크롬 기준): 예제 게임은 약 60개 오브젝트 · 면 120장에서
프레임당 약 17만 블록, 4~8 fps 입니다. 물체가 적은 화면은 20 fps 이상 납니다.

---

## 6. 알아두면 좋은 한계

* 충돌 상자는 **회전하지 않습니다**(축에 나란한 상자/구/경사면만).
  회전한 물체는 보기에는 돌아가지만 충돌은 똑바로 선 상자로 처리됩니다.
* 폴리곤 정렬은 **화가 알고리즘**이라, 아주 크고 길쭉한 면이 서로 파고들면
  앞뒤가 뒤집혀 보일 수 있습니다. 큰 바닥은 `바닥 만들기`(타일)를 쓰세요.
* 붓 채우기에는 투명도가 없습니다(반투명 유리·연기는 표현 불가).
* 한 번에 쓸 수 있는 **오브젝트는 400개**, 모양은 120개, 내가 찍은 점은 3000개,
  면은 1800개입니다. 한도를 넘으면 만들기 함수가 조용히 실패하지 않고
  변수 **`엔진알림`** 에 이유를 적어 줍니다(`만든오브젝트` 는 0이 됩니다).
* **엔트리 리스트 한 개는 5000칸이 최대**입니다. 엔진이 쓰는 리스트는 전부
  4200칸 이하로 잡아 두었고(가장 큰 것이 `pgX`/`pgY` 4200), 빌드할 때 5000칸을
  넘는 리스트가 있으면 에러가 납니다. 내 게임에서 리스트에 항목을 계속 추가할
  때는 5000칸을 넘지 않게 직접 관리하세요(오래된 항목을 지우거나 여러 리스트로
  나눠 쓰기).

---

만든 날짜: 2026-09-20 · 엔진 소스와 빌드 도구: `_work/engine3d/`
