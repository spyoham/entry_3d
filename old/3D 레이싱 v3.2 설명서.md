# 3D 레이싱 v3.2 — 설명서

`3D 레이싱 v3.2.ent`는 **저장 데이터(백업 코드) 불러오기**와 관련된 두 오류를 고친 작은 업데이트입니다. 그 밖의 기능과 조작은 v3.1과 같습니다. 조작은 `3D 레이싱 사용법과 게임 소개.md`를 보세요.

## 무엇이 문제였나
게임의 메뉴는 "방금 새로 눌린 키"를 입력으로 받습니다. 그런데 엔트리가 **실제로는 뗀 키를 계속 눌린 것으로 기억**하는 경우가 있었습니다.

1. **불러온 뒤 조작이 안 됨**
   - 코드를 입력하고 누른 엔터가 게임에도 들어가, 불러오자마자 프로필 화면이 메뉴로 튕겼습니다.
   - 맥에서 Cmd+V로 붙여넣으면 브라우저가 "V를 뗐다"는 신호를 보내지 않습니다. 그래서 엔트리는 V가 계속 눌린 것으로 알고 있었습니다.
   - 이 상태에서 다른 키를 눌렀다 떼면 V가 다시 새로 눌린 것으로 잡혀, 불러오기 창이 계속 다시 떴습니다.
2. **정지 후 다시 실행하면 아케이드 레이스가 저절로 시작됨**
   - 엔트리는 정지해도 눌린 키 목록을 비우지 않습니다.
   - 남아 있던 엔터가 새로 실행하자마자 "새로 눌림"으로 잡혀, 메인 메뉴의 첫 줄(RACE, 기본 아케이드)이 바로 시작됐습니다.

## 이렇게 바꿨습니다
- 게임을 **시작할 때**와 **코드 입력 창(백업 코드, 트랙 공유 코드)이 닫힐 때** 이미 눌려 있는 키는, 한 번 떼기 전까지 무시합니다.
- 정말로 눌린 채 남은 키가 있어도 다른 키는 정상으로 동작합니다. 그 키를 한 번 눌렀다 떼면 다시 쓸 수 있습니다.

## 확인한 것
- `t7/keys.mjs` 검사 3개가 모두 PASS입니다. 고치기 전 v3.1 코드에서는 두 증상이 그대로 재현됐습니다.
  - 엔터가 눌린 채 시작해도 레이스가 시작되지 않습니다.
  - 코드를 불러온 뒤 V가 남고 엔터가 눌려 있어도 프로필에 머물고, 불러오기 창이 다시 뜨지 않으며, ESC와 방향키가 동작합니다.
- tessvm에서 실제 백업 코드를 불러온 뒤에도 조작이 정상입니다.
- 기존 테스트(저장, 동시접속, 백업 코드, 화면, v3.0 기능)가 모두 통과했습니다.
