# 3D 레이싱 v8 — 설명서

`3D 레이싱 v8.ent` 는 v7에 **다른 차 소리**, **서킷별 최고 기록과 고스트**, **혼자 하기(연습 모드)**, **도전과제**, **레벨**, **차 튜닝(차고)**, 그리고 **실시간 변수로 만든 랭킹과 게임 저장**을 더한 버전입니다.
조작과 규칙은 v7과 같습니다(처음이라면 `3D 레이싱 소개.md`).

## 메인 메뉴 (13줄)
| 줄 | 항목 | 새로 바뀐 점 |
|---|---|---|
| 1 | RACE START | |
| 2 | MODE | **PRACTICE**(혼자 하기) 추가 |
| 3 | RULES | |
| 4 | CAR | |
| 5 | **GARAGE** | 새로 추가: 업그레이드와 세팅 (ENTER로 들어감) |
| 6 | CIRCUIT | 카드에 내 최고 랩과 세계 기록 |
| 7 | AI LEVEL | |
| 8 | LAPS → **GHOST / ASSIST** | 타임트라이얼이면 고스트 선택, 연습이면 보조 기능 선택 |
| 9 | WEATHER | |
| 10 | GRAPHICS | |
| 11 | SOUND | |
| 12 | **PROFILE** | 새로 추가: 프로필, 기록, 도전과제, 랭킹 (ENTER로 들어감) |
| 13 | TRACK EDITOR | |

메뉴 제목 옆에 레벨과 닉네임, 경험치 막대가 보입니다.

## 소리 파일은 MP3
온라인 엔트리(playentry.org)는 WAV 소리를 올릴 수 없어서, 작품 안의 소리 13개(내 엔진 1개 + 다른 차 12개)를 모두 **MP3**로 넣었습니다.
작품 크기도 1.19 MB에서 0.99 MB로 줄었습니다. 엔진음 파일만 따로 필요하면 `엔진 소리.mp3`를 쓰세요.

## 1. 다른 차 소리
- 가장 가까운 차 한 대(70 m 안)의 엔진 소리가 들립니다. 그 차의 회전수에 따라 음높이가 다르고, 30 m보다 가까우면 크게, 멀면 작게 들립니다.
- **도플러 효과**가 있습니다. 다가오는 차는 높게, 멀어지는 차는 낮게 들립니다(옆을 지나갈 때 "위이잉").
- 소리 크기는 내 엔진보다 작게(가까울 때 약 절반) 맞췄습니다. 리플레이에서는 보고 있는 차를 기준으로 들립니다.

## 2. 서킷별 최고 기록과 고스트
- 모든 모드에서 규칙에 맞는 랩(트랙 리밋으로 지워지지 않은 랩)을 달리면 그 서킷의 **내 최고 랩**이 갱신됩니다("NEW CIRCUIT BEST!").
- 최고 랩을 세울 때마다 그 랩의 주행이 **고스트**로 저장됩니다(서킷마다 하나, 135초 이내의 랩).
- **TIME TRIAL**의 GHOST 설정:
  - **MY BEST**: 이 서킷의 내 최고 랩 고스트와 달립니다.
  - **WORLD RECORD**: 랭킹 1위의 고스트와 달립니다(온라인). 아직 없으면 내 최고 랩 고스트로 대신합니다.
  - **OFF**: 고스트 없이 달립니다.
- 고스트를 이기면 "GHOST BEATEN!"이 뜨고 도전과제 GHOSTBUSTER를 얻습니다.

## 3. 혼자 하기 (MODE: PRACTICE)
- AI 없이 혼자 서킷을 자유롭게 달립니다. 랩 제한이 없고, 타이어가 닳지 않으며, DRS를 언제나 쓸 수 있습니다.
- ASSIST 설정:
  - **LINE + BRAKES**: 레이싱 라인이 보이고, 코너에 너무 빨리 들어가면 **자동으로 브레이크**를 밟아 줍니다.
  - **LINE ONLY**: 레이싱 라인만 보입니다.
  - **NONE**: 보조가 없습니다.
- **B**: 도로 가운데로 돌아옵니다. **R**: 처음부터 다시 합니다.
- 자동 브레이크가 한 번이라도 작동한 랩은 랭킹에 올라가지 않습니다(내 최고 기록에는 남습니다).

## 4. 레벨
- 경기를 마치면 **경험치(XP)**를 받습니다.
  - 순위: 1위 200, 2위 150, 3위 120 … 8위 50, 여기에 기본 40을 더합니다.
  - 배수: 랩 수(1랩 ×0.75 ~ 10랩 ×3), AI 레벨(ROOKIE ×0.8 ~ INSANE ×1.6), 리얼리스틱(×1.25)
  - 보너스: 깨끗한 경기(지워진 랩·페널티 없음) +30, 도전과제 하나마다 +100
  - 혼자 달리기: 타임트라이얼 랩당 15, 연습 랩당 10, 서킷 최고 기록 +30
- 결과 화면에 받은 XP와 현재 레벨이 나옵니다. 레벨이 오르면 "LEVEL UP!" 알림이 뜹니다.
- 레벨 L에서 L+1로 가는 데 250 + 90×(L−1) XP가 필요합니다. 최대 레벨은 50입니다.
- **레벨이 하나 오를 때마다 업그레이드 포인트를 1점** 받습니다.

## 5. 차고 (GARAGE) — 튜닝
메뉴 5번째 줄에서 ENTER를 누르면 턴테이블 위의 차와 함께 차고가 열립니다.

### 업그레이드 (포인트 사용)
| 부품 | 한 단계마다 | 최대 |
|---|---|---|
| ENGINE | 출력 +1.4%, 최고속 +0.6% | 5단계 |
| AERO | 다운포스 +3% | 5단계 |
| BRAKES | 제동력 +4% | 5단계 |
| TYRES | 그립 +0.8%, 마모 −5% | 5단계 |

→ 로 한 단계 사고, ← 로 되팔면 포인트가 돌아옵니다.

### 세팅 (무료, −3 ~ +3)
| 세팅 | − 쪽 | + 쪽 |
|---|---|---|
| WING | 공기저항 적음 → 직선이 빠름 | 다운포스 큼 → 빠른 코너 그립(한 칸 +6%), 최고속 −1.2% |
| GEARING | 긴 기어 → 최고속 | 짧은 기어 → 가속 +3%, 최고속 −1.5% |
| BRAKE BIAS | 뒤쪽 → 코너에 잘 들어감 | 앞쪽 → 브레이크를 밟을 때 안정 |
| SUSPENSION | 부드러움 → 연석·잔디에서 유리 | 단단함 → 아스팔트 그립 +1% |

화면 아래에 최고속, 가속, 그립, 다운포스, 제동력이 튜닝 반영 수치로 나옵니다. 튜닝은 플레이어 차에만 적용됩니다.

## 6. 도전과제 (20개)
| 이름 | 조건 |
|---|---|
| FIRST FINISH | 경기 완주 |
| WINNER | 우승 |
| PODIUM | 3위 안 |
| POLE POSITION | 예선 1위 (리얼리스틱) |
| COMEBACK | 6번째 이하 그리드에서 출발해 우승 |
| CLEAN RACE | 지워진 랩과 페널티 없이 완주 |
| RAIN MASTER | 젖은 노면에서 우승 |
| PIT PERFECT | 2.5초 안의 피트 스톱 |
| DRIFT KING | 한 번에 드리프트 5000점 |
| TOP SPEED | 340 km/h 돌파 |
| GHOSTBUSTER | 타임트라이얼에서 고스트 이기기 |
| WORLD RECORD | 서킷 랭킹 1위 |
| GLOBETROTTER | 8개 서킷 모두 달리기 |
| CHAMPION | 챔피언십 우승 |
| INSANE | INSANE AI 상대로 우승 |
| MARATHON | 10랩 경기 완주 |
| OVERTAKER | 한 경기에서 5계단 이상 올라가기 |
| ENGINEER | 업그레이드 하나를 최대로 |
| VETERAN | 레벨 10 |
| ROAD TRIP | 누적 500 km 주행 |

얻으면 화면 위에 "ACHIEVEMENT:" 알림이 뜨고 100 XP를 받습니다.

## 7. 프로필 (PROFILE)
메뉴 12번째 줄에서 ENTER를 누르면 열립니다. ←/→로 탭을, ↑/↓로 목록을 넘깁니다.
- **PROFILE**: 닉네임, 레벨, XP, 경기 수·우승·포디움·누적 거리, 도전과제 수, 달린 서킷 수, 업그레이드 포인트, 저장 상태
- **RECORDS**: 서킷마다 내 최고 랩과 세계 기록(기록 보유자)
- **ACHIEVEMENTS**: 20개 목록. 얻은 것은 금색 점이고, 고른 항목의 조건이 아래에 나옵니다.
- **RANKING**: 서킷별 상위 10명(닉네임과 랩타임)과 내 순위

## 8. 랭킹과 게임 저장 (실시간 변수)
**실시간 리스트는 저장이 잘 안 되는 문제가 있어서, 실시간 변수만 사용했습니다.**

| 무엇 | 어디에 | 내용 |
|---|---|---|
| 게임 저장 | 실시간 변수 16개 (`RT_S1`~`RT_S16`) | 닉네임으로 칸을 나눠 한 사람당 한 줄: XP, 업그레이드, 세팅, 도전과제, 통계, 서킷별 최고 랩·최고 경기 |
| 랭킹 | 실시간 변수 8개 (`RT_K1`~`RT_K8`) | 서킷마다 상위 10명의 닉네임과 랩타임 |
| 세계 기록 고스트 | 실시간 변수 8개 (`RT_G1`~`RT_G8`) | 서킷마다 1위 랩의 주행 경로(약 1~2천 자) |

- **닉네임**(엔트리 로그인)으로 사람을 구분합니다. 로그인하지 않으면 게스트이고, 기록은 그 판에서만 유지되며 랭킹에 올라가지 않습니다.
- **불러오기**: 엔트리는 실시간 변수의 서버 값을 작품이 시작된 **뒤에** 보내 줍니다. 그래서 확인용 변수 `RT_SYNC`가 서버 값(`ok`)으로 바뀐 것을 본 뒤에 불러옵니다.
  그 전에 얻은 경험치와 기록은 불러온 값에 **더해집니다.** 아무도 저장한 적 없는 새 작품이거나 오프라인이면 12초 뒤부터 저장을 시작합니다. 메뉴에 "LOADING SAVE..."가 보이는 동안이 불러오는 중입니다.
- **저장**: 메뉴, 결과 화면, 예선 결과, 일시정지, 차고, 프로필처럼 조용한 화면에서 자동으로 저장합니다(3초에 한 번까지).
  저장할 때마다 그 칸을 다시 읽고, 서버에 있는 내 기록과 **합친 뒤**(경험치·통계는 큰 값, 최고 기록은 빠른 값, 도전과제는 모두) 내 줄만 바꿔 씁니다. 다른 사람의 기록은 그대로 둡니다.
- **확인과 재시도**: 쓰고 2초 뒤에 다시 읽어 봅니다. 같은 순간에 다른 사람이 같은 칸에 써서 내 기록이 밀려났으면, 잠깐(무작위로, 점점 길게) 기다렸다가 다시 씁니다. 랭킹도 같은 방식입니다.
- **랭킹 등록**은 주행 중에 바로 쓰지 않고, 결과 화면이나 메뉴 같은 조용한 화면에서 합니다. 엔트리는 실시간 변수를 쓸 때 서버의 대답을 기다리느라 잠깐 멈추기 때문입니다.
- 한 칸이 2400자를 넘으면 **가장 오래 저장하지 않은 사람**의 줄부터 지웁니다(칸마다 약 25명, 전체 약 400명).
- 랭킹은 내 기록이 더 빨라졌을 때만 고쳐 씁니다. 1위가 되면 그 랩의 고스트도 함께 올립니다.

### 저장을 시험하는 방법 (중요)
**편집(만들기) 화면에서는 저장되지 않습니다.** playentry 사이트 코드를 보면, 작품 주인이 편집 화면에서 실행할 때 엔트리가 실시간 변수를 일부러 **오프라인 모드**로 돌립니다(`Entry.cloudVariable.disable()`).
이때 값은 브라우저 안에만 남습니다. 그래서 정지했다 다시 실행하면 남아 있지만, 새로고침하면 사라집니다.

1. 편집 화면에서 작품을 **저장**합니다(실시간 변수가 있는 작품으로 서버에 등록됩니다).
2. **작품 페이지**(`playentry.org/project/작품번호`)를 엽니다. 편집 화면(`/ws/…`)이 아닙니다.
3. 로그인한 상태에서 실행하고, 메뉴 → PROFILE로 들어갑니다. 닉네임이 **코딩재미있어**이면 **TEST +1000 XP** 버튼(클릭 또는 X 키)이 보입니다.
4. 버튼을 눌러 레벨을 올리고 메뉴로 나온 뒤 3초쯤 기다립니다(자동 저장).
5. 페이지를 **새로고침**하고, 메뉴 제목 옆의 LOADING SAVE...가 사라진 뒤 레벨이 유지되는지 봅니다.

작품 페이지에서도 초기화된다면, 사이트가 이 작품을 "실시간 변수가 있는 작품"으로 인식하지 않은 것일 수 있습니다(`hasRealTimeVariable`).
그때는 알려 주세요.

### tessvm 확장을 켜면 저장되지 않습니다
tessvm 확장(0.3.9)은 실시간 변수를 서버에 보낼 때 형식이 엔트리와 다릅니다. 값을 `value`가 아니라 `data`에 담아 보내서, 서버가 저장하지 않습니다.
그래서 게임은 tessvm에서 실행되면 **저장과 랭킹을 읽기만 하고 쓰지 않습니다.** 잘못 쓰면 다른 사람의 저장까지 지울 수 있기 때문입니다.
메뉴에는 "(NOT SAVED)"가, 프로필에는 안내가 나옵니다. **저장하려면 확장을 끄고 실행하세요.**
자세한 원인과 확장 수정안은 `_work/tessvm/tessvm-실시간변수-버그.md`에 있습니다.

### 꼭 알아 둘 점
- 실시간 변수는 **playentry.org에 올린 작품을 온라인으로 실행할 때만** 서버에 저장되고 모두가 함께 봅니다.
  오프라인(엔트리 오프라인 앱, 내려받은 파일)에서는 보통 변수처럼 동작해서, 작품을 멈추면 사라집니다.
- 이 폴더에서는 온라인 동기화를 시험할 수 없었습니다. 오프라인 시뮬레이션으로 두 사용자의 저장·불러오기, 랭킹, 고스트 압축·복원, 칸 용량 제한을 확인했습니다.
  올린 뒤 실제로 저장되는지 한 번 확인해 주세요.
- 실시간 변수는 **나중에 쓴 값이 이깁니다.** 그래서 쓰고 나서 확인하고, 밀려났으면 다시 씁니다(위의 확인과 재시도).
- 동시 접속 시험: 엔트리의 클라우드 변수 코드(entryjs `CloudVariable.js`)와 같은 규칙으로 동작하는 서버 모델을 만들어 시험했습니다(`_work/racing/t7/multi.mjs`).
  쓰기 지연 120 ms와 400 ms, 서버 값이 3초·6초 늦게 오는 경우 모두 통과했습니다.
  - 늦게 온 저장값: XP 5000 + 그 전에 얻은 300 → 5300으로 유지
  - 한 칸에 두 명이 같은 순간 저장 10번: 기록 손실 0
  - 세 명이 같은 서킷 랭킹에 동시에 등록: 3명 모두 등록
  - 네 명이 한 칸에서 1분 동안 무작위로 저장·등록: 4명 모두 최신 기록, 최고 랩 모두 등록
  - 고치기 전 코드는 같은 시험에서 기록을 잃었습니다(XP 5000 → 0, 매번 한 명 손실, 3명 중 2명 손실).
- 실시간 변수 한 개에 들어갈 수 있는 글자 수 제한은 확인하지 못했습니다. 세계 기록 고스트는 약 1.3~2.2천 자입니다.
- 닉네임을 바꾸면 새 프로필로 시작합니다.

## 성능 (tessvm, 싱가포르 ULTRA)
| | v7 | v8 |
|---|---|---|
| 아케이드 레이스 (틱당) | 4.9–5.1 ms | 5.1–5.2 ms |
| 리얼리스틱 레이스 (틱당) | 5.9–6.2 ms | 5.9–6.0 ms |
