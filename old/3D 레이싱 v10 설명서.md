# 3D 레이싱 v10 — 설명서

`3D 레이싱 v10.ent` 는 v9에서 **tessvm 확장으로 실행해도 저장을 시도하도록** 바꾼 버전입니다.
게임 내용·조작·규칙·메뉴는 v9와 같습니다(v9 설명서는 `old/`에, 처음이라면 `3D 레이싱 소개.md`).

## 바뀐 점

v8·v9는 tessvm(0.3.9)에서 실행되면 저장 칸·랭킹·세계 기록 고스트를 **읽기만** 하고 쓰지 않았습니다.
tessvm이 실시간 변수 값을 엔트리와 다른 형식(`data`)으로 보내서 서버에 저장되지 않기 때문입니다.

v10은 tessvm이든 아니든 **항상 저장합니다.** 메뉴의 "(NOT SAVED)"와 PROFILE의 "SAVE IS READ-ONLY" 안내도 없앴습니다.

## tessvm에서 저장하려면

tessvm 제작자가 몇 달 안에 고칠 예정입니다. 그때까지는 **"tessvm 실시간 변수 보정" 확장**(`_work/tessvm-cloudfix`)을 함께 켜야
tessvm에서 저장됩니다. 보정 확장이 메시지 형식을 엔트리 형식(`value`)으로 바꿔 보냅니다.

| 실행 방법 | 저장 |
|---|---|
| 순정 엔트리 | 됨 (v9와 같음) |
| tessvm + 보정 확장 | 됨 |
| tessvm만 | 서버에 저장되지 않음. 게임은 저장 확인이 실패해 "SAVING AGAIN…" 재시도를 이어 갈 수 있습니다 |

tessvm만 켠 채 저장하면 그 저장 칸이 빈 값으로 바뀔 수도 있습니다(서버가 `value`가 없는 값을 받음).
그 칸의 다른 플레이어 기록은 그 사람이 다음에 저장할 때 다시 채워지지만, 그동안은 비어 보일 수 있습니다.

## 시험
- `node build.mjs racing10.ent` → globals 371, lists 409, functions 266 → 265 (저장 차단 함수 `writeOK` 제거)
- `node t7/multi.mjs`, `JIT=300 node t7/multi.mjs`: A–G(늦은 welcome, 같은 순간 저장·랭킹, 4명 60초, 차고 변경, 동시 1위 고스트) 모두 PASS
- 보정 확장: `_work/tessvm-cloudfix/test.mjs` (tessvm 0.3.9의 실제 실시간 변수 코드로 비교) 모두 PASS
- 실제 playentry 서버에서 tessvm + 보정 확장으로 저장되는지는 아직 확인하지 못했습니다(업로드·로그인 필요).
