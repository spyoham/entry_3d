# 3D 레이싱 v6.1 — 설명서

`3D 레이싱 v6.1.ent`는 **느린 컴퓨터에서의 속도**를 높인 업데이트입니다. 게임 내용은 v6.0과 같습니다(서킷 19곳, 피트 스톱 미니게임, 사진 모드, 그래픽 AUTO). 서킷과 기능 안내는 `old/3D 레이싱 v6.0 설명서.md`를 보세요.

## 1. 로딩 화면
메뉴에서 **START**를 누르면 **LOADING** 카드가 먼저 나옵니다. 카드에는 서킷·모드·규칙 이름과 진행 막대가 있습니다. 카드가 떠 있는 동안 경주용 서킷을 만들고, 그래픽에 쓸 값을 미리 계산해 둡니다.
- 예전에는 이 준비 시간 동안 메뉴 화면이 멈춘 채로 있었습니다.
- 느린 컴퓨터에서는 1초 가까이 걸리는데, 이제는 무엇을 하고 있는지 보입니다.

## 2. 더 빨라진 그리기
tessvm은 엔트리의 십진 계산을 그대로 따라 합니다. 그래서 소수가 섞인 덧셈·뺄셈·곱셈은 느린 길을 탑니다. 반면 정수끼리의 계산과 나눗셈은 빠릅니다. v6.1에서는 매 프레임 도는 그리기 계산을 **"정수로 곱하고 더한 뒤, 마지막에 한 번 나누기"** 꼴로 바꿨습니다. 경주 동안 변하지 않는 값은 로딩 때 미리 만들어 둡니다.

- **풍경 시야 판정**: 건물·나무가 화면 밖인지, 너무 작은지 보는 계산을 정수로 바꿨습니다.
  - 물체마다 크기 반경, 방향, 반 크기를 로딩 때 정수로 저장합니다.
  - 블록 건물에서 보이는 벽을 고르는 계산도 같은 방식입니다.
- **깊이와 안개**: 점의 깊이를 정수로 저장합니다. 폴리곤마다 하던 안개 단계 계산이 나눗셈 한 번으로 끝납니다.
- **하늘과 먼 산**: 산 능선 높이표(6°마다)와 각 조각의 방향 탄젠트를 미리 만들어 둡니다. 하늘 띠 14색도 날씨·시간이 바뀔 때만 다시 만듭니다. 매 프레임은 정수 합과 나눗셈 한 번씩만 합니다.
- **자잘한 것**: 먼 옆 구간의 상세도 기준을 프레임마다 한 번만 계산합니다. 회전계 LED 좌표도 정수로 바꿨습니다.

화면은 v6.0과 같습니다. 같은 경주를 시뮬레이터로 프레임마다 비교했을 때 하늘·산 경계에서 몇 픽셀만 달랐습니다.

## 3. 결과
tessvm, HIGH, AI 차를 따라가는 카메라, 서킷마다 두 판을 번갈아 3번씩 재서 평균 냈습니다.

**CPU를 4배 느리게 했을 때** (크롬 CPU 스로틀링. 저사양 컴퓨터 흉내)

| 서킷 | v6.0 | v6.1 |
|---|---|---|
| 모나코 | 25.3 ms/틱 (29.4 fps) | 22.7 ms (32.0 fps), −10% |
| 스파 | 14.5 ms (48.4 fps) | 13.2 ms (51.5 fps), −9% |
| 라스베이거스 | 14.2 ms (48.9 fps) | 13.2 ms (51.3 fps), −7% |

**보통 속도**

| 서킷 | v6.0 | v6.1 |
|---|---|---|
| 모나코 | 5.41 ms/틱 | 4.95 ms, −8.5% |
| 스파 | 3.40 ms | 3.20 ms, −5.7% |
| 라스베이거스 | 3.34 ms | 3.10 ms, −7.3% |

보통 속도에서는 둘 다 60 fps(상한)라 fps는 같습니다. 대신 한 프레임에 남는 여유 시간이 늘어납니다.

풍경이 많은 모나코에서 가장 많이 빨라집니다. 그래픽 AUTO를 쓰면 이 여유만큼 풍경 범위를 덜 줄입니다.

## 4. 시험했지만 넣지 않은 것
- **차 조명 계산 정수화**: 앞 단계 대비 차이가 측정 오차(±1%) 안이었습니다. 차 색이 아주 조금 달라져서 넣지 않았습니다.
- **먼 풍경을 판 한 장으로(임포스터)**: 실제로 그려지는 풍경은 거의 모두 화면에서 반지름 16픽셀 이상입니다(작은 것은 이미 크기 판정으로 빠짐). 판으로 바꾸면 눈에 띄어서 하지 않았습니다.
- **하늘·산을 파노라마 그림 한 장으로**: 하늘·산을 아예 그리지 않는 시험 판으로 상한을 쟀더니, v6.1보다 1~3% 빨라지는 데 그쳤습니다. 서킷 19곳의 그림 파일과 화면 가장자리 왜곡을 감수할 만하지 않습니다.

## 확인한 것
- v3.0 기능 10개, 키, 동시 저장(A–G), 백업 코드, 메뉴 화면 겹침, 차체 기울기(19곳), 벽, 리스트 채우기, 공유 코드, 벽 끼임, 피트 미니게임, 사진 모드·AUTO: 모두 통과.
- 풍경 도로 침범 0, AI 400초 경기 전원 완주, AI 240초 주행(모나코·스파·라스베이거스)에서 1 m 넘는 이탈 0번.
- tessvm에서 로딩 카드와 경주 화면을 직접 찍어 확인했습니다.
- 저장 용량: project.json 15.52 MB (온라인 저장 한도 약 16.17 MB).
