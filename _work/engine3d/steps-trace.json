[{"eval":"(async()=>{const V=n=>Number(Entry.variableContainer.variables_.find(x=>x.name_===n).value_);const L=n=>Number(Entry.variableContainer.lists_.find(x=>x.name_===n).array_[V('player')-1].data);const out=[];let last=-1;for(let i=0;i<160;i++){const f=V('프레임번호');if(f!==last){last=f;out.push([f,+V('프레임시간').toFixed(3),+L('오브젝트Z').toFixed(2),Math.round(performance.now())]);}await new Promise(r=>setTimeout(r,25));}return out.slice(0,26)})()"}]
