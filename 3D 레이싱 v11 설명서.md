# 3D 레이싱 v11 — 설명서

`3D 레이싱 v11.ent` 는 v10에 **백업 코드**를 더한 버전입니다. 온라인 저장(실시간 변수)은 그대로 두고,
저장이 안 될 때를 대비해 플레이어가 자기 기록을 글자로 복사해 두었다가 다시 불러올 수 있습니다.
게임 내용·조작·규칙은 v10과 같습니다(v10 설명서는 `old/`에, 처음이라면 `3D 레이싱 소개.md`).

> 지금 엔트리 서버에 이 작품의 실시간 변수가 등록되어 있지 않아, 순정 엔트리와 tessvm 모두 새로고침하면 기록이 사라집니다
> (`_work/racing/HANDOFF.md`의 v10 "실제 서버 확인"). 엔트리 쪽 문제가 풀릴 때까지 백업 코드가 기록을 지키는 방법입니다.

## 사용법 (PROFILE 화면, 첫 탭)

| 키 | 하는 일 |
|---|---|
| **C** | 백업 코드 복사 |
| **V** | 백업 코드 불러오기 (입력창에 붙여 넣고 Enter) |

- **순정 엔트리:** C를 누르면 **BACKUP CODE** 표 창이 열립니다. 칸의 코드를 선택해 Ctrl+C로 복사하세요.
- **tessvm:** 표를 그림으로 그려서 글자를 선택할 수 없으므로, tessvm의 `$CLIPBOARD` 기능을 씁니다. 화면에 뜨는 확인 창에서 복사를 허락하면 클립보드에 들어갑니다.
- 저장을 불러오는 중("LOADING SAVE...")에는 두 키 모두 기다리라고 안내합니다. 서버 기록과 겹쳐 더해지는 것을 막기 위해서입니다.
- 불러오기는 **코드와 지금 기록 중 더 큰 값이 남는** 방식입니다(온라인 저장의 합치기와 같음). 경험치·통계는 큰 쪽, 랩·경기 기록은 빠른 쪽이 남습니다. 옛 코드를 넣어도 기록이 줄지 않습니다.
- 불러온 뒤에는 온라인 저장도 시도합니다.

## 코드 모양과 조작 방지

```
S 1 GD 9TGEJF2CMAJX … FPPE98PQ XSFD143A8XFDNQ35SW54WJ
│ │ │  └ 본문(암호화된 기록)          └ 검사값 8자
│ │ └ 무작위 소금 2자 (같은 기록도 매번 다른 코드)
│ └ 형식 버전
└ 시작 글자
```

- 글자는 트랙 공유 코드와 같은 32자(숫자 + I·L·O·U를 뺀 영문 대문자)입니다. 대소문자, 띄어쓰기, `-`는 무시합니다. 보통 70–120자이고, 가장 큰 기록도 233자입니다.
- **본문:** 기록 32칸(경험치, 업그레이드, 셋업, 도전과제, 통계, 서킷별 최고 랩·경기)을 닉네임과 소금에서 만든 키로 한 글자씩 밀어 씁니다. 자리마다 32글자 중 13글자만 뜻이 있어서, 한 글자만 바꿔도 대부분 바로 무효가 됩니다.
- **검사값:** 버전, 소금, **닉네임**, 기록 전체로 만든 두 개의 열쇠 해시(각 20비트, 합쳐 40비트)입니다. 지어낸 코드가 통과할 확률은 약 1조분의 1입니다.
- **닉네임에 묶임:** 다른 사람의 코드는 내 계정에서 작동하지 않습니다. 한글 닉네임도 음절표로 글자를 구별합니다(엔트리에는 글자 코드 블록이 없어서 한글 음절 11,172자 문자열에서 위치를 찾습니다).
- **범위 검사:** 업그레이드 0–5, 셋업 −3–+3, 도전과제·서킷 비트 수, 음수 없음 등. 검사값이 맞아도 범위를 벗어나면 거부합니다.
- **한계:** 열쇠는 작품 블록 안에 있으므로, 코드 보기로 블록을 읽고 계산을 따라 할 수 있는 사람은 코드를 만들 수 있습니다. 이 방식은 코드 글자를 고치거나 남의 코드를 쓰는 **가벼운 조작**을 막습니다.

## 시험

| 시험 | 결과 |
|---|---|
| `node t7/savecode.mjs 20000` — 만들기→불러오기, 32칸 모두 되돌아옴 | PASS |
| 다른 닉네임 6개(한 글자 다름, 끝 공백, 같은 길이 한글 등) | 모두 거부 |
| 한 글자씩 바꾼 변형 3,565개 / 잘라 낸 코드 / 글자 추가 | 모두 거부 |
| 무작위 코드 20,000개 | 0개 통과 |
| 옛 코드: 경험치·경기 수·더 빠른 랩 유지, 코드에만 있는 랩은 가져옴 | PASS |
| 엔트리 경로: 표 `svtb` 2행 1열에 코드 + 표 창 열기 / tessvm 경로: `$CLIPBOARD` | PASS (시뮬레이터) |
| **tessvm 하네스**(실제 tessvm 0.3.9): C → `$CLIPBOARD`에 코드, V → 붙여 넣기 → `LEVEL 9, 5000 XP` 불러옴 | PASS |
| tessvm이 만든 코드를 시뮬레이터가 읽음(두 실행기의 계산이 같음) | PASS |
| `node t7/multi.mjs` A–G (동시 저장) | PASS |

- 순정 엔트리의 표 창에서 복사되는지는 실제로 확인하지 못했습니다(블록 동작은 엔트리 소스로 확인). 올린 뒤 C를 눌러 확인해 주세요.

## 만드는 쪽 메모
- 새 파일 `src/savecode.js`, PROFILE 키는 `menu.js profKeys`(C 67, V 86), 글자 칸 38(안내)·39(도움말).
- ejs: `tableSet(표id, 행, 열, 값)` → `set_value_from_table`, `tableShow(표id)` → `open_table`. `pack.mjs`가 `tables`를 받는다.
- `build.mjs`: 표 `svtb`(열 `CODE`, 한 줄), `consts.HANGUL`, `lists.svV`(SVMAX 400).
- 빌드: `node build.mjs racing11.ent` → globals 379, lists 410, functions 275, handlers 3.
