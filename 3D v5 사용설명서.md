# 3D 엔진 v5 — 사용설명서

`3D v5.ent` 는 **엔트리 블록만으로 3D 게임을 만들 수 있게 해 주는 엔진**입니다.
프로젝트를 열면 함수 목록에 **한글 3D 함수 120개**가 들어 있고, `게임` 오브젝트에는
그 함수들로만 만든 **예제 게임 4개(메뉴 포함)** 가 들어 있습니다.

예제를 지우고 자기 게임을 그 자리에 만들면 됩니다. 함수는 프로젝트 전체에서
쓸 수 있으므로 **어떤 오브젝트에서 불러도 됩니다** (단, `3D 장면 그리기` 는 붓을 쓰는
오브젝트 = `게임` 오브젝트에서 부르세요).

## v4에서 달라진 것

| | v4 | v5 |
|---|---|---|
| 오브젝트 최대 | 400개 | **4900개** (엔트리 리스트 한도) |
| .ent 안의 리스트 칸 | 77,300칸 | **3,005칸** (필요한 만큼 실행 중에 늘림) |
| 오브젝트당 리스트 | 41개 | **24개** (플래그·색 묶음, 물리 전용 데이터는 따로) |
| 오브젝트 훑기 | 매 프레임 전부 | **공간 격자**로 카메라 주변만 |
| 새 기능 | — | 트윈, 붙이기(부모-자식), 타이머, 이름표 충돌, 글자 지도, 지형, 블록(복셀), 풍경 뿌리기, 계단 오르기, 미끄러지는 경사, 캡슐 충돌, 회전한 물체 충돌, 궤도·직교·시네마틱 카메라, 디버그 보기 |

---

## 1. 5분 만에 시작하기

```
시작하기 버튼을 클릭했을 때
  🟦3D 엔진 시작하기
  📦바닥 만들기  크기 60  색 [잔디]
  📦상자 만들기  x 0  y 1  z 8   가로 2  높이 2  세로 2   색 [빨강]
  계속 반복하기
    🟦프레임 시작하기
    🎥카메라를  좌우 (0.5)  상하 (0) 만큼 돌리기
    🟦3D 장면 그리기
```

**게임 한 판의 기본 뼈대**는 항상 이 순서입니다.

```
시작하기 버튼을 클릭했을 때
  🟦3D 엔진 시작하기          ← 딱 한 번
  (월드 만들기: 바닥, 지도, 캐릭터 …)
  계속 반복하기
    🟦프레임 시작하기          ← 시간·키보드·트윈·타이머 갱신
    (내 게임 규칙)
    ⚙️물리 계산하기
    🎥카메라 3인칭 …
    🟦3D 장면 그리기
```

---

## 2. 꼭 알아야 할 것

### 좌표계
**x** 좌우, **y** 높이, **z** 앞뒤. 각도는 도(°). `카메라 좌우각` 0° 는 +z 를 봅니다.
오브젝트의 (x, y, z) 는 **물체의 한가운데**입니다(높이 2인 상자는 y를 1로).

### 오브젝트 번호
무엇을 만들든 번호가 붙고, 방금 만든 번호는 변수 **`만든오브젝트`** 에 들어갑니다.

```
📦사람 만들기 x 0 y 2 z 0  키 1.8  색 [파랑]
[내캐릭터] 를 (만든오브젝트) 로 정하기
⚙️오브젝트 (내캐릭터) 의 물리를 1 로 정하기
```

### 색
한글 색 이름이나 `#rrggbb`. 이름 목록은 리스트 `색이름` 에 있습니다:
빨강 주황 노랑 연두 초록 청록 하늘 파랑 남색 보라 분홍 자주 갈색 살구 검정 회색
하양 흰색 금색 은색 잔디 흙 모래 바다 벽돌 돌 나무 밤 어두운회색 밝은회색 크림 민트

### 상태는 리스트로 읽고 씁니다
엔트리의 결과값 함수는 호출당 약 2프레임이 걸리므로, 이 엔진은 **모든 상태를 한글
리스트/변수로 열어 두었습니다.**

| 리스트 | 뜻 |
|---|---|
| `오브젝트X` `오브젝트Y` `오브젝트Z` | 위치 |
| `오브젝트회전X/Y/Z`, `오브젝트크기X/Y/Z`, `오브젝트속도X/Y/Z` | 회전·크기·속도 |
| `오브젝트보임` | 1 보임 / 0 숨김 |
| `오브젝트땅닿음` | 1이면 바닥에 서 있음 |
| `오브젝트닿은것` | 이번 프레임에 닿은 상대 번호 |
| `오브젝트이름표` `오브젝트물리` `오브젝트모양` | 이름표 / 물리 종류 / 모양 번호 |
| `충돌A` `충돌B` | 이번 프레임의 충돌 짝 (`충돌수` 개) |
| `타이머` | 16칸 카운트다운 (0이 되면 끝) |
| `지도` | 글자 지도 (내가 채워 넣는 리스트) |
| `블록저장` | 블록 에디터의 저장 슬롯 |

주요 변수: `프레임시간` `흐른시간` `FPS` `오브젝트수` `그린면수` `보인물체수`
`카메라X/Y/Z` `카메라좌우각` `카메라상하각` `카메라시야각` `중력`
`만든오브젝트` `만든모양` `거리` `방향각` `닿음` `닿은상대`
`광선맞음` `광선오브젝트` `광선거리` `광선X/Y/Z` `화면X/Y/앞`
`높이`(지형) `블록오브젝트` `블록칸X/Y/Z` `지도시작X/Z` `지도골X/Z` `엔진알림`

> ⚠️ 위치를 리스트에 직접 써도 됩니다. 다만 아주 멀리 순간이동시킬 때는
> `🔶오브젝트 위치를 … 로 정하기` 함수를 쓰는 편이 안전합니다(공간 격자가 바로
> 갱신됩니다. 직접 쓴 경우에도 몇 프레임 안에 자동으로 맞춰집니다).

### 움직임은 `프레임시간` 을 곱하세요
`3 × 프레임시간` 처럼 쓰면 느린 컴퓨터에서도 같은 속도로 움직입니다.

---

## 3. 함수 한눈에 보기

### 🟦 엔진
`3D 엔진 시작하기` · `3D 월드 비우기` · `프레임 시작하기` · `3D 장면 그리기`

### 📦 오브젝트 만들기
상자 · 공 · 원기둥 · 원뿔 · 피라미드 · 경사면 · 바닥 · 네모판 · 원판 · 별 · 나무 ·
사람 · 내가 만든 모양 · 복제 · 지우기

### 🔷 모양 디자인
`새 모양 만들기 시작` → `점 추가` → `면 추가(점 4개, 4번째 0이면 삼각형)` →
`모양 완성하기` → `만든모양` 번호로 오브젝트를 만듭니다.
`모양 ( )의 먼 거리 모양` 을 지정하면 멀리서 간단한 모양으로 그려 빨라집니다.

### 🔶 오브젝트 조작
위치·이동·회전·돌리기·크기·색·숨기기·이름표·그림자·빌보드·빛무시·그리기층·
충돌크기·앞으로 가기·바라보기·바닥에 세우기

### 🎞️ 움직임(트윈)과 붙이기 — **새 기능**
```
🎞️오브젝트 (문) 을 1.5 초 동안  x 0  y -3  z 0  로 옮기기  부드럽게 1
🎞️오브젝트 (모자) 을 오브젝트 (플레이어) 에 붙이기  x 0  y 1.05  z 0  회전 0
🎞️타이머 1 번을 120 초로 맞추기        ← 리스트 `타이머` 의 1번이 줄어듭니다
```
트윈은 `프레임 시작하기` 가 자동으로 굴립니다. 부드럽게: 0 직선 / 1 부드럽게 /
2 천천히 시작 / 3 천천히 끝.
붙인 자식은 부모를 따라 움직이고 같이 돕니다(팔에 든 무기, 포탑, 바퀴).

### ⚙️ 물리
`오브젝트 ( ) 의 물리를 ( ) 로 정하기`:

| 값 | 종류 | 쓰임 |
|---|---|---|
| 0 | 없음 | 장식 |
| 1 | 움직임 | 캐릭터·공·총알 |
| 2 | 고정 | 땅·벽·건물·경사면 |
| 3 | 센서 | 통과하지만 닿은 것은 알려 줌 |
| 4 | 움직이는 발판 | 위에 탄 캐릭터가 **같이 이동하고 같이 회전**합니다 |

충돌 모양은 상자(기본) / **공** / **캡슐**(캐릭터용) / 경사면 / 지형이 있고,
**회전한 고정 물체**(비스듬한 벽)도 충돌합니다.
그 밖에: 튕김·마찰·무게·속도·힘·점프·중력·세상 경계·
**자동으로 올라갈 턱 높이(계단)** · **미끄러지기 시작하는 경사**.

닿았는지 묻는 방법 3가지:
```
⚙️오브젝트 (나) 과 (문) 이 닿았는지 검사하기      → 닿음
⚙️오브젝트 (나) 이 이름표 (적) 와 닿았는지 검사하기 → 닿음 / 닿은상대   ← 가장 편함
⚙️오브젝트 (나) 이 점 x y z 에서 5 안에 있는지     → 닿음
```
`⚙️광선 쏘기` 는 `광선맞음 / 광선오브젝트 / 광선거리 / 광선X,Y,Z` 로 답합니다.
(블록이 수천 개여도 격자를 따라가므로 빠릅니다.)

조작: `캐릭터 움직이기(앞뒤·좌우·속도)` · `키보드로 조종하기` ·
`속도·방향으로 달리게 하기(자동차)` · `다른 오브젝트 쪽으로 가게 하기(추적)`

### 🌍 월드 만들기 — **새 기능**
**글자 지도**: 리스트 `지도` 에 한 줄씩 글자를 넣고 부르면 월드가 생깁니다.
```
지도 = [ "#######",
         "#S..*.#",
         "#.###.#",
         "#..T..#",
         "#.W.=.#",
         "#...G.#",
         "#######" ]
🌍글자 지도로 월드 만들기  칸크기 5  벽높이 5  벽색 [벽돌]  바닥색 [잔디]
```
`#` 벽(가로로 이어지면 한 덩어리로 합쳐집니다) · `=` 낮은 벽 · `.` 바닥 ·
`S` 시작(→ `지도시작X/Z`) · `G` 골(센서, 이름표 '골') · `T` 나무 · `*` 별(센서) ·
`W` 물 · 공백은 구멍.

**지형**: `🌍지형 만들기 크기 160 칸수 32 높이 9 거칠기 0.7 씨앗 11`
부드러운 언덕이 생기고 그 위를 걸어다닐 수 있습니다. 먼 조각은 자동으로
간단한 모양으로 바뀝니다. `🌍지형 높이 구하기` 로 아무 지점의 높이를 알 수 있습니다.

**블록(복셀)**: `블록 놓기 / 부수기 / 있는지 보기 / 바닥 깔기`,
`위치 → 블록 칸 구하기`. 칸 좌표로 관리하며, 부수면 충돌도 같이 사라집니다.

**풍경 뿌리기**: `🌍풍경 뿌리기 개수 30 종류 0 범위 150 빈자리 12`
(0 섞기 / 1 나무 / 2 바위 / 3 풀, 지형이 있으면 높이에 맞춰 놓입니다.)

### 🎥 카메라
위치·각도·돌리기·바라보기·시야각·흔들기 · **1인칭** · **3인칭(벽 자동 회피)** ·
**궤도(오브젝트 주위 고정 각도)** · **시네마틱 이동** ·
**보기 방식 0 원근 / 1 직교(쿼터뷰)** · 마우스 드래그 / 방향키 시점

### 🌈 화면·빛
하늘색(위·아래·땅) · 빛 방향과 밝기 · 보이는 거리(안개) · 최대 면 수 ·
먼 거리 모양 기준(크기·거리) · 그리기 방식(면/선/면+선) · 그림자 ·
**디버그(충돌상자) 보기** · 월드 점의 화면 위치

### 🖥️ HUD
네모 · 선 · 막대(체력바) · 조준점 · 글 8칸(위치·색 지정, 월드 좌표 위에 띄우기)

### 📐 계산
두 오브젝트/두 점 사이 거리 → `거리`, 방향각 → `방향각`

---

## 4. 들어 있는 예제 4개

메뉴에서 ↑↓ 로 고르고 ENTER, 게임 중에는 ESC 로 메뉴.

1. **별 모으기(플랫포머)** — 글자 지도로 만든 스테이지, 걸어 올라가는 경사면,
   회전하며 오르내리는 발판, 트윈으로 열리는 문, 머리에 붙인 모자(부모-자식),
   쫓아오는 적, 1·3인칭 전환.
   `WASD 이동 · SPACE 점프 · C 시점 · 마우스드래그/←→ 시점`
2. **사격 게임(1인칭)** — 지형 + 풍경, 다가오는 적, 광선으로 사격, 체력바, 탄약,
   무리(wave)가 계속 늘어납니다. `F 발사 · R 재장전`
3. **레이싱** — 체크포인트를 순서대로 통과, 랩타임과 최고 기록.
   `W 가속 · S 브레이크 · A D 조향`
4. **블록 만들기(에디터)** — 날아다니며 블록을 놓고 부수기, 색 바꾸기,
   리스트로 저장/불러오기, 직교 보기, 디버그 보기.
   `WASD·Q·E 이동 · F 놓기 · R 부수기 · 1 색 · S 저장 · L 불러오기 · O 직교 · B 디버그`

---

## 5. 빠르게 만드는 요령

속도는 **화면에 그리는 면의 수**가 결정합니다. 느리면 이 순서로 줄이세요.

1. `🌈보이는 거리를 ( ) 로 정하기` 를 줄인다 (예: 110 → 70). 제일 효과가 큽니다.
2. `🌈그림자 0` 또는 `그림자가 보이는 거리` 를 줄인다.
3. `🔷먼 거리 모양` 을 지정한다(공·나무·사람·원기둥·지형은 이미 되어 있음).
   `🌈먼 거리 모양으로 바꾸는 거리` 를 줄이면 더 일찍 바뀝니다.
4. `🌈한 화면 최대 면 수` 를 줄인다(넘는 면은 그리지 않습니다).
5. 면이 많은 모양(공·나무) 대신 상자·피라미드를 쓴다.

**오브젝트 수 자체는 겁내지 않아도 됩니다.** 공간 격자 덕분에 1500개짜리 블록
월드도 100개짜리 장면과 같은 속도로 돕니다(화면에 보이는 면만 그리므로).

참고 수치(헤드리스 크롬): 메뉴 11 fps · 플랫포머 6~7 fps(면 115) ·
사격 6~7 fps(면 163) · 블록 에디터 5 fps(면 239) · 블록 1575개 월드 5 fps.

---

## 6. 알아두면 좋은 한계

* 충돌은 축에 나란한 상자/공/캡슐/경사면/지형, 그리고 **Y축으로 돌린 고정 물체**까지입니다.
  기울어진(X·Z 회전) 물체는 보기에는 돌아가지만 충돌은 똑바로 선 상자로 처리됩니다.
* 폴리곤 정렬은 화가 알고리즘이라, 아주 큰 면끼리 파고들면 앞뒤가 뒤집혀 보일 수
  있습니다. 큰 바닥은 `바닥 만들기`(자동으로 타일로 쪼갭니다)를 쓰세요.
* 붓 채우기에 투명도가 없어 반투명·연기는 표현할 수 없습니다.
* 블록 월드는 가려진 면도 그립니다(면 단위로 숨기지 않습니다).
* **한 리스트는 5000칸까지.** 엔진이 쓰는 리스트는 전부 그 안에 들어가고, 빌드할 때
  검사합니다. 오브젝트는 **4900개**, 모양 200개, 점 4900개, 면 4900개,
  움직이는 물체 1200개까지. 한도를 넘으면 조용히 실패하지 않고 변수 **`엔진알림`**
  에 이유가 적힙니다.
  내 게임에서 리스트에 항목을 계속 추가할 때도 5000칸을 넘지 않게 관리하세요.

---

만든 날짜: 2026-09-20 · 엔진 소스와 빌드 도구: `_work/engine3d/` (HANDOFF.md 참고)
