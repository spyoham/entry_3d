# ENTRY RACING 3D — 기술 설명

엔트리에는 3D 기능도, 게임 엔진도 없습니다. 이 작품은 **엔트리 기본 블록과 붓(펜) 블록만으로** 3D 레이싱 게임을 만든 것입니다.
화면의 도로·건물·차·하늘·지도는 전부 매 프레임 붓으로 다각형을 채워서 그리고, 이미지 파일은 쓰지 않습니다.

| | |
|---|---|
| 함수 | 266개 |
| 변수 / 리스트 | 372개 / 409개 |
| 오브젝트 | **2개** (펜 1개, 글상자 1개 + 복제본) |
| 시작 블록 | 3개 |
| 한 프레임에 그리는 면 | 약 400~900개 |

---

## 1. 개발 방식 — 자바스크립트에서 엔트리 블록으로

블록을 손으로 조립하지 않고, **자바스크립트의 일부 문법으로 게임을 쓴 뒤 직접 만든 컴파일러로 엔트리 블록을 만들어 냈습니다.**

- 대응 관계
  - `let x` → 변수, `let A = []` → 리스트
  - `function` → 엔트리 함수, `on('start', …)` → 시작 블록
  - `while` / `if` / 사칙연산 / 삼각함수 / 리스트 읽기·쓰기 → 대응하는 블록
  - `let 오브젝트$이름` → "이 오브젝트에서 사용" 변수(복제본마다 따로 갖는 변수)
- 엔트리의 성질에 맞춘 처리
  - **반복문을 한 프레임 안에 끝내기**: 엔트리의 반복 블록은 한 바퀴마다 화면을 갱신하며 쉽니다. 조건 자리에 "계속 반복하기" 블록을 넣는 요령으로 쉬지 않고 돌게 만듭니다.
  - **값을 돌려주는 함수는 쓰지 않기**: 엔트리에서 값 반환 함수는 부를 때마다 프레임이 걸립니다. 결과를 전용 변수에 담는 일반 함수로 씁니다.
  - **자주 쓰는 변수를 앞에**: 엔트리는 변수를 목록에서 차례로 찾으므로, 가장 많이 쓰는 변수와 리스트를 목록 앞에 둡니다.
  - **`&&`, `||`는 단락 평가를 하지 않습니다**: 리스트 범위를 벗어나는 읽기는 스레드를 조용히 멈추게 하므로, 겹친 `if`로 보호합니다.
  - **빈 문자열 `''`은 `0`과 같다고 비교됩니다**: 글자를 지울 때는 보이지 않는 폭 0 문자를 씁니다.
- 같은 코드를 **보통 자바스크립트로도 변환**할 수 있어서, 엔트리 없이 게임 전체를 시뮬레이션하고 그림을 찍어 볼 수 있습니다. 물리·AI·경기 규칙의 수치는 대부분 이 방법으로 수백 초씩 돌려 측정해서 맞췄습니다.

---

## 2. 3D 렌더러 — 붓 채우기로 그리는 3D

### 기본 파이프라인
1. **카메라 설정**: 카메라의 앞·오른쪽·위 방향 벡터를 요(yaw)·피치·롤로 만듭니다.
2. **구간 선별**: 트랙은 같은 간격의 링(단면) 460개로 되어 있습니다. 카메라 앞쪽으로 시야 안에 드는 구간만 고릅니다.
3. **거리 정렬(화가 알고리즘)**: 먼 구간부터 가까운 구간 순서로 칠합니다. 트랙을 따라 걸으면 거의 정렬된 순서로 나오므로, 헤어핀처럼 순서가 뒤집힌 곳만 삽입 정렬로 고칩니다.
4. **투영**: 필요한 링의 점만 카메라 좌표로 바꾸고 거리로 나눠 화면 좌표를 얻습니다. 두 구간이 공유하는 링은 한 번만 계산합니다.
5. **그리기**: 면마다 뒷면 검사, 화면 밖 검사를 거쳐 붓으로 채웁니다.

### 깊이 문제 해결
- **건물과 차는 옆 트랙 구간과 함께** 그립니다. 풍경 오브젝트를 구간별 연결 리스트에 매달아 두면, 구간을 칠하는 순서가 곧 그리는 순서가 되어 도로·건물·차의 앞뒤가 저절로 맞습니다.
- **근평면 자르기(Sutherland–Hodgman)**: 카메라 바로 앞을 지나는 면은 잘라서 그립니다. 관중석 안이나 터널 속을 지나가도 화면이 깨지지 않습니다.
- **뒷면 제거**: 화면에서 시계 방향으로 그려지는 면은 뒤를 보고 있으므로 그리지 않습니다(부호 있는 넓이 검사).
- **차 모델의 그리기 순서**: F1 차는 볼록하지 않아 뒷면 제거만으로는 순서가 맞지 않습니다. 미리 **8방향 각각의 뒤→앞 면 순서**를 계산해 두고, 카메라가 차를 보는 방향(45° 단위)에 맞는 순서를 고릅니다.

### 줄이는 기법 (LOD와 선별)
- 멀리 있는 도로는 링 2~3개를 한 칸으로 합치고, 멀수록 도로 가장자리만 투영합니다.
- 건물과 나무는 **두 단계 모델**(단순 / 자세히)을 가지고, 가까운 것만 자세히 그립니다.
- 차는 가까운 몇 대만 전체 모델, 나머지는 실루엣 모델, 아주 멀면 색 카드 한 장입니다.
- 건물·차는 **경계구(bounding sphere)로 시야 절두체 검사**를 해서, 화면 밖이면 정점 계산 전에 버립니다.

### 색과 빛
- 재질 240가지 × 안개 16단계의 **색표**를 서킷을 불러올 때 한 번 만들어 둡니다. 면마다 거리로 안개 단계를 정해 색을 한 번 찾기만 합니다.
- 차는 면의 법선과 해의 방향으로 밝기를 매번 계산하고 RGB 블록으로 색을 만듭니다. 그래서 어떤 도색이든 색표 없이 그릴 수 있습니다.
- 먼 산과 도시 스카이라인은 실제 지형이 아니라, **월드 방위각으로 색인한 높이 프로필**을 화면에 그린 띠입니다. 카메라가 돌면 제자리에 붙어서 지나갑니다.
- 반투명(타이어 연기, 물보라, 고스트 차)은 채우기 색이 투명도를 받지 않아서 **붓 투명도 블록**으로 만듭니다.

### 정수 고정소수점 계산
엔트리의 사칙연산은 `0.1 + 0.2 = 0.3`이 되도록 **십진수로 정확하게** 계산합니다. 실행기(특히 tessvm)는 이 동작을 흉내 내느라, 비슷한 크기의 소수끼리 빼는 계산(`161.86 − 147.66` 같은)이 정수 계산보다 수십 배 느립니다. 3D 변환은 거의 전부가 이런 계산입니다.

그래서 변환 전체를 **정수**로 합니다.
- 월드 좌표는 **cm 단위 정수**, 건물 모델은 cm, 차 모델은 mm 정수입니다.
- 카메라 축 벡터는 **16384배** 해서 정수로 만듭니다.
- 모델 → 화면 변환을 오브젝트마다 **정수 행렬 하나**로 합쳐서, 정점마다 정수 곱셈·덧셈 9번만 합니다.
- 화면 좌표는 **1/16 픽셀 단위 정수**라서 뒷면 검사와 화면 밖 검사도 정수 연산입니다. 붓에 넘길 때만 16으로 나눕니다.

같은 화질·같은 시야 거리에서 가장 무거운 장면(야간 시가지, 최고 그래픽)의 프레임 시간이 약 **13~21ms → 5~6ms**로 줄었습니다.

### 리스트 5000칸 제한
엔트리 리스트는 5000칸까지입니다. 트랙 링 461개 × 점 10개, 차 모델, 계산용 칸, 풍경 모델 칸을 하나의 정점 버퍼(4938칸)에 빈틈없이 배치했습니다.

---


### 작품 용량 (v3.3)
엔트리 온라인은 작품 데이터가 약 16 MB를 넘으면 저장하지 못합니다. 그래서 처음에 0만 들어 있는 큰 리스트(리플레이·정점 버퍼·서킷 링·타이어 자국·고스트 등 105개, 14만 칸)는 작품에 비워 두고, 시작 스크립트 첫 줄의 `allocLists()`가 같은 길이로 채웁니다. 빌드가 이 함수를 자동으로 만듭니다(`build.mjs` ALLOCMIN). project.json은 12.4 MB입니다(v4.0: 실제 서킷 풍경 문자열이 더해져 12.5 MB).

## 3. 서킷 만들기

### 실제 지도에서 (v4.0)
8개 서킷은 빌드 전에 실제 자료로 만듭니다(`_work/racing/real/`). 인터넷에서 한 번 받아 두고(`fetch.mjs`, 캐시는 git 제외), `prep.mjs`가 `real/circuits.json`을 만듭니다. `build.mjs`는 이 파일이 있으면 그 서킷을 씁니다.
- **자료**
  - 중심선: bacinger/f1-circuits(MIT). 경도·위도를 서킷 가운데 기준 미터로 바꿉니다(x 동쪽, z 북쪽. 게임의 오른쪽 법선과 맞아서 뒤집히지 않습니다).
  - 주변: OpenStreetMap(ODbL)의 건물, 관중석, 숲·공원·가로수, 바다·호수·강·항구, 경주로 자체(`highway=raceway`), 이름 붙은 명소.
  - 높이: SRTM 30 m(OpenTopoData). 랩을 따라 8 m마다, 서킷 둘레 60 m 격자, 12 km까지의 원 64방향.
- **중심선 다듬기**: 손으로 딴 중심선은 20–60 m마다 점이라 코너가 몇 번의 꺾임입니다. 지도에 경주로가 촘촘히 그려진 곳은 같은 방향으로 달리는 14 m 안의 경주로 점으로 끌어당기고(끌림은 랩을 따라 부드럽게), 나머지(시가지 서킷의 일반 도로)는 조금 더 세게 다듬습니다. 제어점은 코너에서 촘촘하고(5 m) 직선에서 성깁니다(26 m).
- **출발선**: 메인 스트레이트 옆 피트 레인의 가운데와 나란한 지점. 방향이 같아야 합니다(모나코는 피트 레인이 출발 직선과 반대로 달리는 수영장 구간 사이에 있습니다).
- **높이**: 중앙값과 평균으로 다듬은 뒤, 공식 고저차(스파 104 m 등)로 늘리거나 줄이고, 경사를 19% 안으로 제한합니다. 터널은 위쪽 지형을 무시하고 입구와 출구를 잇습니다.
- **터널과 다리**: 시가지 서킷이 큰 건물 아래를 지나면 터널입니다(모나코 페어몬트 호텔). 8자 서킷은 가장 가깝게 교차하는 곳을 찾아 윗길을 11.8 m 위로 올립니다.
- **풍경 레코드**: 물체마다 모델, 랩 위치, x·z, 지면 기준 높이, 방향, 가로·높이·세로 크기, 색, 상세 등급, 겹침 검사 여부를 64진 21글자로 적어, 서킷마다 몇 개의 긴 문자열로 넣습니다(리스트 5000칸 제한과 저장 용량 때문). 트랙을 불러올 때 `placeReal`이 풀어서 자기 링에 매답니다.
  - 건물: 바닥 다각형의 최소 사각형(볼록 껍질 회전). L자처럼 사각형을 반도 못 채우면 긴 변을 반으로 나눕니다. 높이는 `height`, 층수×3.2 m, 없으면 종류별 기본값. 보이는 크기/거리 순으로 서킷당 220–760채. 모든 건물은 길이 1 m짜리 상자 모델(`block`)을 늘린 것이라, 렌더러가 축마다 다른 배율을 받습니다(`scK/scKY/scKZ`).
  - 물: 20 m 격자에서 물 다각형 안이거나 해안선의 바다 쪽(해안선 오른쪽)인 칸을 모아 최대 140 m 사각형으로 합칩니다.
  - 나무: 숲 13 m, 공원 26 m 간격에 흔들어 심고, 멀수록 솎아 냅니다.
  - 지면 높이: 격자 높이와 도로 높이의 차이를 서킷마다 정한 비율로 줍니다(SRTM은 숲과 건물 윗면까지 재므로 줄여서 씀). 건물은 5 m 땅에 묻어서 비탈에서 뜨지 않습니다.
  - 상세 등급: LOW는 약 1/3, HIGH는 2/3, ULTRA는 전부. 지도와 중심선이 몇 m 어긋나 도로에 걸친 것은 링에서 바깥으로 밀고, 안 되면 버립니다.
- **먼 산**: 원 64방향의 높이로 6°마다 지평선 각도를 구합니다. 2.5 km 안은 앞 능선, 그 밖은 뒤 능선입니다(지구 곡률 반영).

### 링과 자동 생성
- **(옛 방식) 지도처럼 적기**: 에디터 서킷과 예전 판은 "코너 꼭짓점 좌표 + 반지름"으로 적고, 한 바퀴가 정확히 닫히도록 직선 길이를 **최소제곱법**으로 조정합니다.
- **링 만들기**: 곡선(캐트멀-롬 스플라인)을 같은 간격의 링 460개로 다시 나눕니다. 링마다 도로 가장자리, 연석, 런오프, 잔디, 벽 높이 등 점 10개를 만듭니다. 실제 길이라 링 간격은 모나코 7 m에서 스파 15 m까지입니다.
- **자동 생성**
  - 곡률에 따라 뱅크, 연석, 자갈·아스팔트 런오프, 타이어 배리어가 생깁니다.
  - 가장 긴 직선 두 곳은 DRS 구간이 됩니다.
  - 입체교차(스즈카의 다리)는 윗길 높이와 벽을 따로 둡니다.
- **레이싱 라인**
  - 각 링의 주행선이 양옆 링을 잇는 직선 쪽으로 조금씩 끌려가는 과정을 90번 반복합니다(곡률 최소화).
  - 그 곡률로 구간별 최고 속도를 구하고, 제동 한계로 거꾸로 훑어 제동 구간을 계산합니다.
- **AI가 보는 곡률 (v4.0)**: 링 곡률은 앞뒤 링 사이 평균이라 링이 15 m 간격이면 짧은 헤어핀을 실제보다 완만하게 봅니다. 그래서 스플라인 세밀 샘플(약 6 m)에서 링마다 가장 급한 곡률(`sgCurvA`, 평균의 1.6배 + 0.002 이하)을 따로 구해 AI 코너 속도에 씁니다. AI의 조준점과 앞보기 거리도 링 수가 아니라 미터(링 8.5 m 기준)로 계산하고 링 사이를 보간합니다.
- **다른 구간 그리기 (v4.1)**: 그릴 링은 카메라 링에서 뒤로 4개, 앞으로 안개 거리까지 훑어 고릅니다. 그 밖의 링(랩의 다른 부분)도 카메라에서 LOW 180 m / HIGH 300 m / ULTRA 450 m 안이고 화면 쪽이면 그리기 단위로 더합니다(`gfSide`). 먼 링은 한 링에 링 간격 이상 가까워질 수 없으니 (거리 − 반경) / 링 간격만큼 건너뛰어 싸게 훑습니다. 모든 단위는 거리순으로 다시 정렬되어 화가 알고리즘 순서가 맞습니다.
- **지형 (v4.2)**: `prep.mjs`가 서킷 둘레를 30–40 m 격자(모서리 4900개 이하)로 나눠, 땅 높이 T(x,z) = (지형 비율 × 흐린 SRTM + (1 − 비율) × 근처 랩 고도의 거리 가중 평균)을 게임 높이로 바꿉니다. 그래서 두 구간 사이에서 땅이 튀지 않고, 들어 올린 스즈카 윗길 옆도 원래 땅입니다. 칸마다 토지 이용(숲/시가지/땅)과 등급(교차로 380 m 안 2 = HIGH, 나머지 3 = ULTRA)을 1글자로, 높이를 2글자로 넣고, 잔디 띠 안쪽·물·건물이 35% 넘게 덮은 칸은 뺍니다. 게임은 3×3칸 조각을 각자 그리기 단위로 깊이 정렬에 넣고(`visI` 음수), 4칸 안은 칸마다, 그 밖은 네 모서리 한 장으로 그립니다. 정점은 풍경 모델 슬롯을 빌려 투영합니다(`drawLand`). 건물·나무 높이도 같은 T를 씁니다.
- **다리 (v4.2)**: 다리 링(32)은 잔디 점을 도로 끝 1.3 m 아래로 옮겨, 잔디 사각형이 그대로 교량 옆면이 됩니다. 밑면은 다음 링도 다리일 때만 그립니다. 제방 링(64)은 잔디 바깥 끝이 땅 높이로 내려갑니다(모든 그래픽). 다리와 그 양옆 링은 한 링씩 그리고(여러 링을 한 단위로 묶으면 옆면이 들판까지 늘어남), 다리 아래 도로(128)는 정렬 거리를 늘려 먼저 그립니다.
- **잔디 띠 폭 (v4.2)**: 링마다 좌우로 2 m씩 나가며 다른 구간(랩에서 35 m 넘게 떨어진 곳)의 도로에 4 m 안으로 닿으면 거기서 멈춥니다(`ctlSL/SR`). 런오프도 그 안으로. 띠 끝 45 m 안에 더 낮은 구간이 있으면 그 높이까지 비탈로 내립니다.
- **풍경 겹침 검사**: 놓을 때마다 그 모델의 **실제 바닥 크기(회전과 배율 포함)**가 서킷 **어느 구간과도** 겹치지 않는지 검사합니다. 멀리 떨어진 링은 거리만큼 건너뛰며 검사해서 빠릅니다. 가까운 곳은 링 사이 도로도 1/4 간격으로, 런오프는 앞뒤 링 중 넓은 쪽으로 잽니다(v4.1). 터널 위 건물은 터널 지붕(7 m) 위에서 시작합니다. 검사 도구: `t7/intrude.mjs`(RUNOFF=1이면 런오프·피트 레인 포함).

---

## 4. 차량 물리

- **자전거 모델**: 앞바퀴와 뒷바퀴의 미끄러짐 각도를 따로 계산합니다. 그래서 오버스티어, 언더스티어, 드리프트가 자연스럽게 나옵니다.
- **그립 = 기계적 그립 + 다운포스**: 다운포스가 속도의 제곱에 비례해서, 빠른 코너는 빠르게 돌 수 있지만 헤어핀은 그렇지 않습니다.
- **마찰원**: 세게 브레이크를 밟는 타이어는 옆으로 버틸 힘이 줄어듭니다.
- **슬립 한계**: 미끄러짐 각도가 한계를 넘으면 그립이 오히려 줄어서, 핸들을 과하게 꺾으면 차가 밀려납니다.
- **운동량 보존**: 차가 돌 때 속도 방향이 공짜로 따라 돌지 않고, 타이어 힘으로만 방향이 바뀝니다.
- **노면**: 도로·연석·잔디·자갈·아스팔트 런오프·피트 레인마다 그립과 구름저항이 다르고, 점프대에서는 공중에 뜹니다.
- **충돌**: 차끼리는 원이 아니라 차 모양의 긴 상자로 계산해서, 얕은 쪽 축으로 밀어냅니다.
- **벽과의 충돌**: 벽 **쪽으로** 들어가는 속도만 없애고 살짝 되튕깁니다. 앞으로 가는 속도는 부딪친 세기에 비례한 마찰만큼만 줄고, 세게 박으면(벽 쪽 속도 5 m/s 초과) 더 줄어듭니다. 벽을 긁으며 달리면 초당 약 25%씩 줄어듭니다. 모두 시간 기준이라 프레임 수와 상관없습니다(216 km/h에서 0.5°로 스치면 0.5초 뒤 213 km/h, 30°로 박으면 약 100 km/h).
- **차체 기울기** (v2.6): 차가 선 자리의 **경사(트랙 방향 오르내림)와 뱅크(가로 기울기)**를 차의 앞 방향과 오른쪽 방향으로 나눠서, 앞뒤 기울기(피치)와 좌우 기울기(롤)를 만듭니다. 그래서 오르막에서는 코가 들리고, 옆으로 선 차는 옆으로 기울고, 뱅크 코너를 비스듬히 지나면 두 방향으로 함께 기웁니다. 여기에 코너에서 바깥으로 쏠리는 롤, 브레이크에서 코가 숙여지고 가속에서 뒤가 가라앉는 피치를 더합니다. 콕핏·T캠 카메라도 차체를 따라 기웁니다. 8개 서킷의 가장 가파른 곳과 가장 기운 뱅크에서, 차 앞뒤·좌우 1 m 지점의 실제 노면 높이로 잰 각도와 1° 안으로 맞습니다(`t7/tilt.mjs`).
- **바퀴별 타이어** (v2.6, 리얼리스틱): 바퀴 네 개마다 온도·마모·그립이 있습니다. 0.1초마다(차마다 조금씩 어긋나게) 가로 가속도로 바깥 바퀴 하중을, 브레이크로 앞 하중을, 저속 가속으로 뒤 하중을 나누고, 앞·뒤 미끄러짐 각도, 드리프트, 브레이크 잠김, 연석을 더해 바퀴마다 "일"을 구합니다. 온도는 일의 제곱근만큼 오르고(서킷마다 온도가 너무 벌어지지 않게), 속도·젖은 노면·잔디에 따라 식습니다. 마모는 같은 일에 비례하고, 작동 구간을 넘으면 크게, 많이 차가우면 조금 더 빨리 닳습니다. 차의 그립은 네 바퀴 평균이고, 앞 축과 뒤 축이 평균보다 좋은지 나쁜지를 앞·뒤 타이어 힘에 따로 곱합니다. 그래서 앞이 상하면 언더스티어, 뒤가 상하면 오버스티어가 됩니다. 8개 서킷에서 미디엄의 평균 온도는 87–103°C입니다(`t7/whcal.mjs`).
- **v3.0 리얼리스틱 차량 시스템** (`src/race3.js`, 타이어와 같은 0.1초 시계)
  - **브레이크 온도**: `+9·페달·속도 − 0.08·(0.3+v/60)·팀 냉각·(T−기온)` °C/s. 250°C 아래와 950°C 위에서 제동력이 줄어듭니다(`caBrD`). 브레이크 빛(`caHeat`)도 이 온도로 정합니다.
  - **락업**: 제동 요구량을 브레이크 밸런스로 앞뒤에 나누고, 축마다 `타이어 그립 × 2.4`를 넘으면 잠긴 것으로 봅니다. 잠긴 축은 제동이 80%로 줄고 옆 그립이 45~55%로 떨어집니다. 잠긴 정도와 속도에 따라 바퀴마다 **플랫스팟**(`whFS`, 최대 0.15)이 쌓이고, 그만큼 그립이 줄고 마모가 늘며 플레이어 화면이 떨립니다. AI는 실수할 때를 빼면 잠기지 않을 만큼만 밟습니다.
  - **연료와 무게**: 소모는 `스로틀 × 속도`, 즉 스로틀을 밟고 달린 **거리**에 비례합니다. 서킷마다 랩타임 추정이 달라도 한 랩 소모가 일정합니다. 스로틀 거리 비율 0.72는 `t7/fuel.mjs`로 쟀습니다. 연료 100 kg은 무게 +10%이고, 가속·제동은 그대로 나누고 옆 그립은 절반만큼 줄이며 앞 그립을 최대 3% 줄입니다. 연료 모드는 출력·최고속과 소모를 바꿉니다. AI는 남은 랩보다 연료가 모자라면 코너 전에 스로틀을 떼고 굴러갑니다(리프트 앤 코스트).
  - **고장**: 경기당 약 4.5%×팀 신뢰성의 확률로 여섯 가지 중 하나가 납니다. RICH 모드와 1050°C 넘는 브레이크에서 확률이 오릅니다. 리타이어하면 `caDNF`가 되고 진행도에서 60000을 빼 순위 맨 뒤로 갑니다. AI 차는 동력 없이 굴러 갓길로 빠진 뒤 섭니다.
- **v3.0 노면**: 고무(`trkRub`)는 빠르게 달리는 차 수에 비례해 쌓이고 비에 씻깁니다. 그립은 `(0.975 + 0.035·고무) × 온도 보정`입니다. 노면 온도는 서킷 기본값에서 경기 진행, 저녁, 젖음에 따라 내려갑니다. 비는 16구역마다 강도·도착 속도·마르는 속도가 다르고, 드라이 라인(`zLine`)은 비가 그친 뒤 달리는 차 수만큼 자랍니다. 차 밑의 물(`caWet`)은 구역 값에서 레이싱 라인(`rlO`) 3 m 안이면 줄이고, 터널에서는 10%만 남깁니다. 타이어 그립, 식는 속도, 트레드 타이어 과열은 모두 이 차별 값을 씁니다.
- **v3.0 세팅**: 앞/뒤 윙은 다운포스 총량과 **앞뒤 배분**(`caFWb`)을 바꿉니다. 배분은 속도에 따른 공력 비율만큼 앞·뒤 그립에 반대로 곱합니다. 디퍼렌셜은 저속 파워 오버스티어, 스로틀을 뗀 코너의 앞 그립, 뒷타이어 발열·마모를, 공기압은 그립·발열·마모·구름저항을 바꿉니다.
- **저프레임 대응**: 물리 계산을 55ms 이하 조각으로 잘게 나눠 돌려서, 초당 몇 프레임밖에 안 나오는 환경에서도 같은 속도로 달리고 AI가 벽을 뚫지 않습니다.
- **시간 보정**: tessvm은 시계가 "프레임 수 × 1/60초"로 가서, 60프레임을 못 내는 컴퓨터에서는 게임 전체가 느려집니다. 컴퓨터 시계의 초 단위 날짜 블록으로 실제 초당 프레임을 세어 시간 배율을 맞춥니다.

---

## 5. AI

- **목표 속도**: 앞에 있는 코너마다 "돌 수 있는 속도"와 "지금 속도로 그 코너 전에 줄일 수 있는가"를 계산해서, 가장 낮은 값을 씁니다.
- **조준점 거리 제한**: 반지름 R인 코너에서 d만큼 앞을 보고 달리면 안쪽으로 d²/8R만큼 파고듭니다. 이 값이 도로를 벗어나지 않게 조준 거리를 줄입니다.
- **성격**: 공격성(추월 시도 거리, 늦은 브레이크), 수비(코너 안쪽 한 번 막기), 실수 확률(코너 앞에서 그립을 과대평가)을 드라이버마다 다르게 줍니다. 랩마다 페이스도 조금씩 흔들립니다.
- **레이스 운영**: 타이어 마모와 날씨에 따른 피트 전략, 황색기 구간 감속, 세이프티카 뒤 줄서기(앞차와의 간격으로 속도 조절)를 합니다.
- **예선 기록**: 차 성능으로 계산한 이상적인 랩타임에, 실제로 측정한 AI 랩타임과의 비율을 곱해 만듭니다.

---
- **v3.1 벽에 끼인 차 복구**: 레이스 중 AI가 2.5 m/s 아래로 6초 넘게 있으면(피트 정차·리타이어·완주 제외, 피트 출구는 15초) 같은 자리의 도로 가운데 쪽으로 옮겨 트랙 방향 6 m/s로 다시 출발시킵니다. 후진 탈출이 벽에 긁혀 실패하고, 도로 밖 복구가 잔디·자갈에서만 작동하던 빈틈을 메웁니다.
- **v3.0 AI**: 앞차에 1.6초 안으로 붙고 타이어가 38% 넘게 닳았으면 공격성에 따라 먼저 피트에 들어갑니다(언더컷). 바로 뒤 차가 피트에 들어가면 따라 들어가고(커버), 앞차가 들어가면 기준을 30%에서 22%로 낮춰 더 버팁니다(오버컷). 세이프티카와 VSC 때는 타이어가 반쯤 닳았으면 들어옵니다. 1초 안에 뒤차가 붙어 있으면 실수 확률이 1.8배가 되고, 실수는 늦은 브레이크(락업 가능)나 코너 바깥으로 밀려나기입니다. 블루 플래그를 받으면 6% 늦추고 라인을 비켜 줍니다. VSC 중에는 레이싱 라인 속도의 62% 이하로, 포메이션 랩에서는 그리드 순서대로 최대 145 km/h, 앞차와 18 m를 두고 달린 뒤 자기 칸까지의 거리로 멈춥니다.

## 6. 차 모델

- F1 차를 단면이 다른 상자(로프트) 여러 개와 육각형 바퀴로 만들었습니다: 노즈, 앞날개(플랩·엔드플레이트), 사이드포드, 헤일로, 헬멧, 에어박스, 디퓨저, 뒷날개.
- 두 단계 모델: 실루엣(면 38개)과 전체(면 95개).
- 카메라와 해의 방향을 **차 자신의 좌표로** 옮겨 두면, 면마다 뒷면 판정과 밝기 계산이 내적 하나로 끝납니다.
- 앞바퀴는 핸들만큼 돌고, 브레이크등과 비 오는 날의 후미등이 켜집니다. 부서진 앞날개는 그 면만 빼고 그립니다.

---

## 7. 글자 — 글상자 하나와 복제본

오브젝트 수를 줄이기 위해, 화면의 모든 글자를 **글상자 하나와 그 복제본**으로 씁니다.

- 시작할 때 글상자가 **자기 복제본 80개**를 만듭니다. 복제본마다 "이 오브젝트에서 사용" 변수로 자기 칸 번호를 기억합니다.
- 게임은 글자·위치·크기·색을 리스트의 칸에 적고, 바뀐 칸의 **버전 번호**만 올립니다. 복제본은 자기 칸의 버전이 바뀌었을 때만 다시 씁니다. 그래서 가만히 있는 글자는 비용이 거의 없습니다.
- 엔트리의 "크기 정하기"는 글상자의 폭에 따라 달라지는데, 보통 글상자는 글자 수에 따라 폭이 변합니다. **줄바꿈 글상자는 폭이 고정**이라, 크기가 정확한 글자 배율이 됩니다.
- 왼쪽 정렬은 정확하고, 가운데·오른쪽 정렬은 고정폭 글꼴의 글자 폭으로 계산합니다. 표의 값은 왼쪽 정렬 열로 두어 글꼴이 달라도 흐트러지지 않습니다.
- 글상자 배경색을 **투명**으로 명시합니다(지정하지 않으면 흰 배경으로 그리는 실행기가 있습니다).
- 개발 중에는 "정지 화면에서 매 프레임 다시 쓰이는 칸"을 찾는 검사로 칸 충돌을 잡았습니다.

---

## 8. 메뉴와 3D 미리보기

- **카테고리 메뉴**: 메뉴 행에는 항목 번호가 들어 있고, 페이지(최상위 / 레이스 설정 / 차와 차고 / 설정)마다 행 목록을 다시 만듭니다. 항목을 바꾸는 코드는 페이지와 상관없이 항목 번호로 동작합니다.
- **카드 속 3D 차**: 월드를 다 그린 뒤, 카메라 상태를 저장하고 가상의 차와 전용 카메라로 바꿔 그리고, 화면 좌표를 카드 위치로 옮긴 다음 카메라를 되돌립니다.
- **회전하는 3D 서킷 지도**: 서킷 중심선을 64개 점으로 줄여 단위 원 안에 맞추고, 높이를 과장한 뒤 돌리고 기울여 약한 원근으로 그립니다. 받침판, 그림자 띠, 높이에 따라 밝아지는 도로 띠, 출발 깃발 순서로 칠합니다.
- **차고 턴테이블**: 쇼룸의 차는 월드를 다 그린 뒤 마지막에 그려서, 가까운 도로 면에 가려지지 않습니다.
- **성능 그래프**: 차의 수치(최고속, 0-100·0-200 km/h, 속도별 횡가속)는 게임 물리식을 그대로 적분해서 구합니다. 그래서 표의 값과 실제 주행이 일치합니다.
- **메뉴 배경 카메라**: 눈과 조준점을 모두 링 사이에서 연속으로 보간하고, 방향을 부드럽게 따라가게 해서 링을 지날 때 시선이 튀지 않습니다.

---

## 9. 연출

- **리플레이**
  - 모든 차의 위치를 0.1초마다 **순환 버퍼**(550샘플 × 9대 = 4950칸)에 기록합니다.
  - 재생 전에 실제 경기 상태를 저장해 두고, 끝나면 되돌립니다.
- **TV 카메라**: 서킷마다 코너 바깥쪽에 자동으로 배치되고, 차와의 거리로 시야각(줌)을 정합니다.
- **불꽃과 파편**: 고속에서 바닥 긁힘, 벽·차 충돌, 착지 때 입자를 뿌립니다.
- **해 질 녘**: 경기가 진행될수록 하늘과 안개 색을 저녁 쪽으로 옮기고, 변화가 일정 이상 쌓일 때만 색표를 다시 굽습니다.
- **비**: 노면 젖음과 비의 세기를 따로 두어, 그립·시야·물보라·하늘을 각각 바꿉니다.

---

## 10. 엔진 소리

- 녹음이 아니라 **직접 합성한 소리**입니다: V6 엔진의 폭발 주파수와 배음, 불규칙한 폭발의 울림, 터보 휘파람, 흡배기 잡음을 섞은 1.5초짜리 반복음입니다.
- 온라인 엔트리에 올릴 수 있도록 MP3로 넣었습니다. MP3 디코더가 앞뒤에 붙이는 무음만큼 **다음 반복을 앞당겨** 이음매가 들리지 않게 합니다.
- **소리 재생 속도** 블록으로 음높이를 회전수에 맞춥니다(아이들 ~ 레드존).
- **주변 차 소리**: 재생 속도와 크기는 작품 전체에 하나뿐이라 다른 차의 음높이를 따로 줄 수 없습니다. 그래서 음높이 6단계 × 거리 2단계 = 12개의 짧은 소리를 미리 합성해 두고, 가장 가까운 차의 회전수 비율과 **도플러 효과**로 맞는 소리를 골라 틉니다.

---

## 11. 공유 코드

- 서킷 노드를 **32진법(Crockford Base32)** 글자로 압축합니다.
  - 위치는 4 m, 높이는 2 m, 폭은 0.5 m 단위로 줄입니다.
  - 터널·점프·방호벽·체크포인트는 비트로 묶습니다.
  - 노드 하나가 7글자에 담깁니다.
- 끝에 검사 숫자 2글자를 붙여 한 글자만 틀려도 알아냅니다. `O`/`0`, `I`·`L`/`1`처럼 헷갈리는 글자는 같게 읽습니다.
- 입력은 **묻고 대답 기다리기** 블록으로 받습니다.

---

## 12. 레벨·저장·랭킹 — 실시간 변수

### 저장 구조
- 엔트리의 **실시간 변수**는 서버에 저장되고 모든 사용자가 같은 값을 봅니다. 실시간 리스트는 저장 누락이 잦아서 **변수만** 썼습니다.
- 사람마다 한 줄(`|닉네임,경험치,업그레이드,세팅,도전과제,통계,서킷별 기록…`)을 만들고, **닉네임의 해시로 16개 저장 칸**에 나눠 담습니다. 한 칸은 여러 사람의 줄을 이어 붙인 문자열입니다.
- 랭킹은 서킷마다 변수 하나에 상위 10명의 `|닉네임,밀리초`를 담습니다.
- 세계 기록 고스트는 0.25초마다 찍은 위치를 첫 점만 절대좌표로, 나머지는 0.2 m 단위 **정수 차이값**으로 32진법 글자에 담습니다. 되돌려도 오차가 쌓이지 않고, 차의 방향은 경로에서 다시 계산합니다.
- 엔트리 블록은 변수를 번호로 고를 수 없어서, 칸 번호로 변수를 고르는 부분은 `if` 사슬입니다.

### 실시간 변수의 성질과 대응
엔트리 소스를 분석해 확인한 동작입니다.
- 값을 쓰면 서버가 받아들인 뒤에야 내 쪽 값이 바뀝니다.
- 작품을 켠 직후에는 서버 값이 아직 도착하지 않아 **작품에 들어 있는 기본값**이 읽힙니다.
- 여러 사람이 쓰면 **나중에 도착한 값이 이깁니다**(last-write-wins). "내가 읽은 값 그대로일 때만 쓰기" 같은 기능은 없습니다.

그래서 다음처럼 설계했습니다.
1. **도착 확인 후 불러오기**: 확인용 변수가 서버 값으로 바뀐 것을 본 뒤에 불러오고, 그 전에 얻은 경험치와 기록은 불러온 값에 **더합니다.**
2. **읽고 → 합치고 → 쓰기**: 저장 직전에 칸을 다시 읽고, 서버의 내 줄과 합칩니다(경험치·통계는 큰 값, 기록은 빠른 값, 도전과제는 합집합). 그다음 **내 줄만 바꾸고 다른 사람 줄은 그대로** 씁니다.
3. **확인과 재시도**: 2초 뒤 다시 읽어 **내가 쓴 줄이 글자 그대로 있는지** 봅니다. 동시에 쓴 다른 사람이 옛 값으로 덮었으면, 무작위로 점점 길게 기다렸다가(지수적 백오프와 비슷함) 다시 합니다. 덮은 사람의 기록도 칸에 남아 있으므로 결국 모두의 기록이 남습니다.
4. **랭킹도 같은 방식**: 등록 → 확인 → 재시도입니다. 서버에 쓰는 동안 잠깐 멈추므로, 주행 중이 아니라 결과·메뉴 같은 조용한 화면에서 씁니다.
5. **고스트와 랭킹의 일치**: 두 사람이 거의 동시에 1위를 세우면, 고스트 변수에 2위가 된 사람의 고스트가 남을 수 있습니다. 1위는 확인 단계에서 고스트의 이름·시간이 자기 것인지 보고 아니면 다시 올립니다. 불러올 때도 랭킹 1위와 맞는 고스트만 씁니다.
6. **용량**: 방금 저장한 사람의 줄이 맨 뒤로 가고, 칸이 2400자를 넘으면 가장 오래된 줄부터 지웁니다(LRU).

엔트리 클라우드 변수와 같은 규칙으로 도는 **서버 모델**(메시지 지연과 무작위 지연 포함)을 만들어 시험했습니다. 같은 순간 저장, 여러 명의 동시 랭킹 등록, 4명이 한 칸에서 1분 동안 무작위로 저장하는 경우 모두 기록이 사라지지 않았습니다.
오프라인에서는 실시간 변수가 보통 변수처럼 동작하므로, 같은 코드가 그 판 안의 저장으로 그대로 돌아갑니다.

---

## 13. 실행기별 대응 (엔트리 / tessvm)

tessvm은 엔트리 작품을 자바스크립트로 바꿔 실행하는 크롬 확장 프로그램입니다. 실행기를 직접 분석해서 다음을 맞췄습니다.

| 성질 | 대응 |
|---|---|
| 십진수 사칙연산 흉내가 느림 | 3D 변환을 정수 고정소수점으로 |
| 시계가 프레임 수 기준 | 실제 초당 프레임을 세어 시간 배율 보정 |
| 배경색 없는 글상자를 흰 배경으로 그림 | 투명 배경 명시 |
| 채우기 색의 8자리 색상값(투명도)을 검정으로 처리 | 붓 투명도 블록 사용 |
| 실시간 변수 쓰기가 서버에 전달되지 않는 버그(확장 0.3.9) | tessvm에서는 저장을 읽기 전용으로. 실행기 쪽 수정안도 정리 |

tessvm에서 게임이 자기가 tessvm 위에 있는지는 실행기가 1로 정해 주는 특별한 변수로 알아냅니다.
순정 엔트리에서는 같은 작품이 초당 2~3프레임으로 돌고, 물리 계산을 잘게 나눠 게임 속 시간은 실제 시간대로 흐릅니다.

---

- **눌린 채 남은 키 (v3.2)**: 엔트리는 정지해도 눌린 키 목록(`Entry.pressedKeys`)을 비우지 않습니다. 맥의 Cmd+V처럼 브라우저가 키를 뗀 신호를 보내지 않는 경우도 있습니다. 그래서 메뉴 키는 시작할 때와 코드 입력 창이 닫힐 때 이미 눌린 키를, 한 번 떼는 것을 볼 때까지 무시합니다(`main.js` `pkSt`, `keysStale`).

## 14. 검증 방법

- **시뮬레이터**: 게임 코드를 보통 자바스크립트로 돌리고, 붓 채우기를 픽셀로 직접 칠해 그림으로 저장합니다. AI끼리 수백 초씩 경주시켜 코스 이탈, 타이어 마모, 피트 전략, 세이프티카 대열을 숫자로 확인했습니다.
- **실제 실행기 자동 조작**: 헤드리스 크롬에서 엔트리와 tessvm으로 작품을 돌리며 키를 누르고, 화면을 찍고, 프레임 시간과 함수별 CPU 사용량을 쟀습니다. CPU를 일부러 4배 느리게 해서 느린 컴퓨터도 시험했습니다.
- **정적 검사**: 풍경과 트랙의 겹침, 글자 칸 충돌, 메뉴 카메라의 흔들림(프레임 사이 방향 변화)을 수치로 검사했습니다.
